# Handoff — BA รุ่นยืนยัน H3/H4/H5 และกราฟตาม accept CSV

## สถานะปัจจุบัน — 11 กันยายน 2569

ผู้ใช้ให้บันทึกโค้ดรุ่นที่ยืนยันแล้วทับ root โดยตรงและ commit/pull/push ขึ้น GitHub จากนั้นจะรัน VB6 เอง ให้ตรวจ git log เพื่อดู commit ล่าสุด ฐานก่อนงานนี้คือ d84d218 บน main ที่ origin=https://github.com/dohcivil-ui/RC_Wall_Fix.git

ใช้ modBA.bas SHA256 `e6ab8b41b990fb1f2cb0133adff26c1df027c1e8d0d368671632f2bb5babb6d4` รุ่น adaptive-coupling ซึ่งผ่านชุดยืนยัน seed142345–142374 30คู่/ความสูง: H3/fc240=26/30 (86.67%), H4/fc280=24/30 (80%), H5/fc320=21/30 (70%) ทั้งสองวิธีผ่านทุกTrial งบ5000evaluationsรวมinitial เกณฑ์นับชนะคือราคาBA≤HCAจากค่าดิบและBAพบราคาสุดท้ายของตนก่อนHCA ใช้BAโค้ดเดียวกันทั้งสามกรณี ไม่อ้างว่าทุกชุดสุ่มจะได้ผลเท่ากัน GUIใช้seedจากเวลาครั้งเดียว ไม่ได้จับคู่seedแบบชุดยืนยันอัตโนมัติ

คำสั่งล่าสุดของผู้ใช้: “คงแบบ max และบันทึก Reject ตามจริง” ให้ใช้ข้อกำหนดนี้แทนการวาดราคา initial ด้วยเส้นประใน e73c024 ราคาและสถานะเริ่มต้นไม่เปลี่ยน: H3=29,924.364480 H4=33,912.911680 H5=38,041.658880 บาท/ม. เริ่ม tt=.60 tb=1.00 TBase=1.00 B=7.00 toe=1.20 heel=4.80 เมตร และ DB28@.10 ทั้งสามชุด No.0 ถูก Reject ตามชุดตรวจปัจจุบัน

Form1.frm และ modGraphing.bas อ่านกราฟจาก accept CSV ของ Trial ที่เลือกโดยตรง คง 4 คอลัมน์ No.,Rejected,Passed,Passed and Better value เฉพาะคอลัมน์สุดท้ายอัปเดตเส้น best และคงค่าผ่านเดิมในแถวอื่น ก่อนคำตอบผ่านครั้งแรกยังไม่มีเส้น ไม่เติมราคา Reject/เลื่อนเลข No./วาดเส้นอ้างอิง initial การพบราคาดีขึ้นจริงที่ปัดแล้วเท่าราคาเดิมยังเก็บ No. ของแถวนั้นเป็นรอบดีที่สุด กราฟเดี่ยวและเปรียบเทียบใช้ renderer เดียวกัน สี BA น้ำเงิน/HCA ส้ม พร้อมตัดแกน X และเครื่องหมายบอกรอบดีที่สุด เส้นประที่จุดดีที่สุดเป็น guide เท่านั้น

แก้ helper พล็อต Python ให้ใช้กติกาเดียวกัน ไม่ส่งไฟล์ประวัติหรือเพิ่มคอลัมน์ใหม่ คงการส่งออก accept/loopPrice/selectedTrial/BMP เดิม สูตร หน่วย ขอบเขต BA/HCA/RNG และวิธีเลือก Trial ไม่เปลี่ยน ผู้ใช้จะเปิด RC_RT_HCA_v2.vbp ใหม่แล้ว F5 เอง ไม่เปลี่ยน EXE เดิม

ตรวจ regression 15 ข้อผ่านและคอมไพล์ GUI ผ่าน: audit/csv-graph-fix-20260911/ready/runtime.txt และ ready-gui/compile.log ไม่รัน optimizer หรือ 30 trials เพิ่ม Replay CSV ต้นแบบตรง BA No.32/HCA No.326 ราคา 2,849.23 ส่วนผลผู้ใช้ปัจจุบันตรง BA No.102/HCA No.327 ราคา 3,076.92 และเริ่มผ่าน No.44/124 ตามจริง ผลปัจจุบันต่างจากชุดยืนยันเดิมและไม่ได้เขียนทับ

ผลยืนยัน BA เดิม: audit/ba-paired-win-20260910/adaptive-coupling-refinement/confirmation/comparison-H3-H4-H5-30pairs.csv และ summary-all-heights.json ไม่รับประกันผลชุดสุ่มใหม่

รักษาresult_csvทั้งหมด901ไฟล์ตาม snapshot ก่อนแก้กราฟและรายการที่ผู้ใช้ลบ ห้ามstage/คืนผล/ลบ/จัดระเบียบเอง CSVและBMPใช้ชื่อเดิมทับได้ที่ C:\reserch 69\RC_Wall_Fix\result_csv เมื่อผู้ใช้รันVB6เท่านั้น Preserve legacy source bytes/CRLF/no BOM ไม่เปลี่ยนสูตร/อัลกอริทึม/หน่วย/ขอบเขตหรือรันทดลองใหม่โดยไม่มีคำสั่ง

## ประวัติก่อน checkpoint นี้ — ไม่ใช่โค้ดหรือผลล่าสุด

ข้อความด้านล่างเก็บลำดับการทดลองเก่า รวมผลที่ไม่ผ่านและcandidateที่เลิกใช้ ข้อความ “ล่าสุด/สถานะปัจจุบัน” ในประวัติหมายถึงเวลานั้น ให้ยึดสถานะด้านบนและNEXT_CHAT_PROMPT.mdเสมอ

# Handoff — version revise BA pass with resersh

## ผล H3 max initial วิธีละ30ครั้งล่าสุด — 10 กันยายน 2569

ผู้ใช้สั่ง “เริ่มทดลองเลยสิ เริ่มจาก h=3 f'c=240 30ครั้งก่อน” รันแล้ว BA30/HCA30 งบ5000 รวม60ครั้ง300000evaluations เริ่มcatalogue maxร่วมผ่านuseSharedInitเดิม ราคาเริ่ม29,924.364480เท่ากันทุกครั้ง คงsource/Fixed boundsเดิม ผล BAไม่พบแบบผ่านทั้ง30ครั้ง; HCAผ่าน30/30 ราคา2,942.341920ทุกครั้ง เลือกTrial27 Loop200 (evaluation201), Loopเฉลี่ย473.033333 SD224.696805 ผลอยู่ [รายงานH3ล่าสุด](audit/h3-max-initial-30-2026-09-10/REPORT.md) พร้อมtraceทุกTrial/CSV/verification ไม่มีการแก้productionหรือรันH4/H5เพิ่ม ผลเดิมresult_csv890ไฟล์ไม่เปลี่ยน

BAติดกลไกข้อเสนอเมื่อเปลี่ยนcurrentเป็นcatalogue maxแต่Fixed boundsยังตามHเดิม: ttใกล้.60แต่tbถูกclamp<=.35, midpointมีheel.05 และdirected-centerติดREVERSED_BASE_FACE ทุกTrialทำครบ5000 ไม่ใช่หยุดเพราะinitialReject ผลNO_SOLUTIONห้ามนับLoop0เป็นคำตอบเร็วหรือใช้sentinelเป็นราคา และไม่มีpaired t-test30คู่ของรอบพบคำตอบในชุดนี้ ไม่รันซ้ำชุดเดิมเพื่อเลือกผลชนะ ต้องพิจารณาความเข้ากันของinitializer/กลไก/ขอบเขตก่อนแก้หรือทดลองรุ่นต่อไป

## ข้อชี้แจงและทดสอบจุดเริ่มต้นล่าสุด — 10 กันยายน 2569

ผู้ใช้ต้องการเริ่มขนาดสูงสุดร่วมกัน แล้วค้นหาต่อแม้ initial ถูกReject ยืนยันว่าโค้ดปัจจุบันทำต่อหลังRejectจริง: รับแบบผ่านครั้งแรก แล้วรับราคาต่ำลง ไม่จำเป็นต้องมี feasible initial คำอธิบายก่อนหน้าที่บอกว่าแบบRejectใช้เริ่มไม่ได้ต้องแก้ตามนี้

ตรวจจุดเริ่มต้นเดิมและ catalogue max แล้วราคาBA/HCAเท่ากัน ดู audit/equal-initial-price-2026-09-10 ต่อมาทดสอบค้นหาจาก catalogue max ด้วย useSharedInit เดิม วิธีละ1ครั้งต่อH3/H4/H5 งบ5000 รวม6ครั้ง ผล BA H3/H4ไม่พบแบบผ่าน ส่วน HCAพบครบ และH5ทั้งสองพบ โดยคงsource/Fixed boundsเดิม ไม่มีการติดตั้งinitializerหรือเปลี่ยนสูตร/domain ผลอยู่ [รายงานค้นหาหลังReject](audit/search-after-rejected-start-2026-09-10/REPORT.md) ไม่ควรขยายผลจากหนึ่งครั้งเป็นข้อสรุป30ครั้ง การทดลองนี้พบความไม่เข้ากันระหว่างจุดตั้งต้นmaxทั้งตารางกับกลไกข้อเสนอBA/ขอบเขตตามHเดิม ต้องพิจารณาก่อนเปลี่ยนproduction ผลเดิมresult_csv890ไฟล์ไม่เปลี่ยน

## อัปเดตการส่งออกล่าสุด — 10 กันยายน 2569

ผู้ใช้สั่งให้ CSV ใช้ชื่อเดิมทับได้ ไม่สร้างสำเนา และบันทึกภาพผลใน root เดียวกัน จึงแก้การส่งออกให้ใช้ `C:\reserch 69\RC_Wall_Fix\result_csv` เสมอ ไม่มี timestamp/เลขท้าย/การสร้าง archive ใหม่ ทั้ง CSV และภาพทับชื่อเดิมเมื่อผู้ใช้รันกรณีเดิม คำสั่งนี้แทนข้อกำหนดเก่าที่ให้ archive ก่อนทับ แต่ไม่ได้สั่งลบ archive หรือคืนผลเก่า

หลังจบ BA/HCA แต่ละชุด บันทึกภาพหน้าตัดจาก frmBestDesign เป็น `design-BA-H3-240.bmp` และกราฟจาก picGraph เป็น `convergence-BA-H3-240.bmp` (เปลี่ยนวิธี/H/fc ตามจริง) ภาพใช้ native PictureBox.Image รวมข้อความ/เส้นที่วาดใน AutoRedraw; ชื่อไม่ใส่ Trial เพื่อทับชื่อเดิม แต่ภาพหน้าตัดแสดง Selected trial และ selectedTrial CSV ระบุ No. กรณีไม่มีคำตอบ ภาพหน้าตัดแสดง NO_SOLUTION และกราฟที่เคลียร์แล้วถูกบันทึกทับ ป้องกันภาพผลเก่าค้าง ภาพกราฟคงเลข evaluation ที่แสดงบนจอ; CSV Loop ยังเริ่ม0ตามเดิม

แก้เฉพาะ modShared.bas, modBatch.bas (เส้นทางไฟล์ batch_step3/batch_step3_A3 ให้ทับชื่อเดิม), Form1.frm และ frmBestDesign.frm; modBA.bas/modHillClimbing.bas เหมือนก่อนงานนี้ทุกไบต์ คอมไพล์ root ผ่าน และ native fixtures ผ่าน54ข้อ ไม่รัน optimizer เพิ่ม ตรวจไฟล์ผลที่มีอยู่ตอนเริ่มงานนี้888ไฟล์พร้อมhashว่าไม่เปลี่ยน ไม่ลบ/คืนไฟล์ใด ผู้ใช้เปิด VBP ใหม่แล้ว F5 เอง ดู [รายงานส่งออกล่าสุด](audit/fixed-result-export-2026-09-10/REPORT.md)

## สถานะปัจจุบันหลังติดตั้ง BA — 10 กันยายน 2569

ผู้ใช้สั่งล่าสุด “งั้นแก้โค้ดเลยผมจะรันเอง” จึงติดตั้ง candidate ที่ผ่านการยืนยันลง `modBA.bas` ใน root ตรงทุกไบต์แล้ว คอมไพล์ root sources ผ่าน โดยเก็บ Check.exe ใน audit เท่านั้น ไม่เปลี่ยน EXE เดิม ไม่รัน optimizer เพิ่ม ให้ผู้ใช้เปิด `RC_RT_HCA_v2.vbp` ใหม่ใน VB6 แล้ว F5 อ่าน [หลักฐานการติดตั้ง](audit/ba-directed-center-installation-2026-09-10/REPORT.md) และรอผลจากผู้ใช้

ผู้ใช้แก้เกณฑ์เป็น **ราคา BA ≤ HCA และรอบ BA < HCA** ใน Trial ที่เลือก (ราคาต่ำสุดก่อน แล้วรอบน้อยสุดเมื่อราคาเท่ากัน) ครบ H3/H4/H5 และสั่ง “ทดสอบอีกหาวิธีทำให้ได้” จึงสร้าง candidate เดียวที่เพิ่มข้อเสนอแบ่งครึ่งไปทางขนาด/คู่เหล็กน้ำหนักต่ำกว่าใน neighbor เป็นระยะ โดยไม่เปลี่ยน HCA สูตร หน่วย ราคา domain งบ หรือชุดตรวจ

candidate อยู่ `audit/ba-directed-center-2026-09-10/candidate-modBA.bas`, SHA-256 `6caad1ffb401bde91873ef5bdd1d6ab046bfda7e82e07ddc797ac860c7c14b5c` **ติดตั้งลง modBA.bas ใน root แล้วและ hash ตรงกัน** อ่าน [รายงานพัฒนาและยืนยัน](audit/ba-directed-center-2026-09-10/REPORT.md) ก่อนดำเนินการต่อ

คัดกรอง BA10ต่อความสูง รวม30Trial/150,000evaluations แล้วตรึงไฟล์เดิม ยืนยันใหม่ BA30/HCA30ต่อความสูง รวม180Trial/900,000evaluations โดยแยกข้อมูลสองชุด ผลยืนยันผ่านเกณฑ์กราฟทั้งสาม: H3 BA Trial2 Loop10 / HCA Trial6 Loop107 ราคาเท่ากัน2,942.34; H4 BA Trial1 Loop140 / HCA Trial17 Loop186 ราคาเท่ากัน4,985.56; H5 BA Trial4 Loop447 / HCA Trial29 Loop1064 ราคาเท่ากัน8,538.42 ไม่ได้แปลว่าชนะทุก Trial หรือทุกชุดสุ่มในอนาคต

ชุดยืนยันอยู่ `audit/ba-directed-center-confirm-2026-09-10` มี full traces, all-trials.csv, accept/selectedTrial/loopPrice ของแต่ละกรณี, กราฟ7×4ซม.600dpi, verification93ข้อ และ mechanism-verification63ข้อ ตรวจ directed proposals111,960ครั้ง ข้อมูลคัดกรองอยู่ `audit/ba-directed-center-screen-2026-09-10` แยกชัดเจน ไม่รวมเป็นnของชุดยืนยัน Rootsource/binariesไม่เปลี่ยนจากก่อนการทดลองนี้ และ result_csvเดิม519ไฟล์พร้อมinventoryไม่เปลี่ยน ไม่มีcommit/push ยังไม่แก้บทความ

เกณฑ์ “ราคาต่ำกว่าอย่างเคร่งครัด” ในประวัติด้านล่างเป็นเกณฑ์เก่า ให้ใช้ ≤ สำหรับราคา และ < สำหรับรอบตามสถานะปัจจุบันนี้ กลไกใหม่อธิบายตามเงื่อนไข `EvaluationCount Mod 4 = 2` ขณะอยู่ในช่วง neighbor ไม่ใส่ราคา/มิติคำตอบเฉพาะความสูงและไม่แก้ข้อมูลกราฟให้ชนะ

## ประวัติการทดลองก่อนพัฒนา candidate

สถานะล่าสุด 10 กันยายน 2569: ผู้ใช้เปลี่ยนเกณฑ์กราฟเป็น BA ต้องได้ราคาต่ำกว่า HCA อย่างเคร่งครัด และพบราคานั้นเร็วกว่าพร้อมกันครบ H3/H4/H5 โดยเลือก Trial ราคาต่ำสุดก่อน แล้วรอบน้อยสุดเมื่อราคาเท่ากัน ผู้ใช้สั่ง “ทดลองเลย” อนุญาตให้รัน optimizer ใน audit แล้ว จึงทดสอบรุ่นปัจจุบัน BA/HCA วิธีละ 30 ครั้งต่อกรณี งบ 5,000 รวม 180 Trial/900,000 การประเมิน ไม่มีการปรับกลไกค้นหาหรือรันซ้ำเลือกผล

ผลชุดใหม่นี้ไม่ผ่านทั้งสามกรณีตามเกณฑ์สองข้อร่วมกัน: H3 ราคาเท่ากัน 2,942.34, BA Trial10 Loop89 กับ HCA Trial28 Loop68; H4 ราคาเท่ากัน 4,985.56, BA Trial24 Loop154 กับ HCA Trial21 Loop228; H5 ราคาเท่ากัน 8,538.42, BA Trial17 Loop323 กับ HCA Trial21 Loop903 รุ่นปัจจุบันยังไม่ทำให้ BA ได้ราคาต่ำกว่า HCA ในกรณีใดของชุดนี้ อย่าใช้ค่าเฉลี่ยมาทดแทนเกณฑ์กราฟที่ผู้ใช้ยืนยัน และยังไม่ได้พิสูจน์ราคา global optimum

เก็บข้อมูลครบทุก Trial, trace, protocol, source snapshots, กราฟและ [รายงานผลล่าสุด](audit/strict-graph-2026-09-10/REPORT.md) ผลทดสอบอยู่ audit/strict-graph-2026-09-10 เท่านั้น ผ่านการตรวจ 93 ข้อรวม native Double/ลำดับ/งบครบทุกเวกเตอร์ root source/binaries ไม่เปลี่ยนจากก่อนทดลอง และ result_csv เดิมทั้ง 519 ไฟล์พร้อม inventory ไม่เปลี่ยน ยังไม่มี candidate BA ใหม่หรือการติดตั้งเพิ่มเติม

อัปเดต 10 กันยายน 2569: ผู้ใช้อนุญาตเปลี่ยน accept สำหรับกราฟให้เป็น Trial ที่ดีที่สุดแยกต่อวิธี เลือกราคาสุดท้ายต่ำที่สุดก่อน ถ้าราคาเท่ากันเลือกรอบที่ถึงราคานั้นน้อยที่สุด หากเท่ากันทั้งหมดเก็บ Trial แรก การแก้นี้อยู่เหนือ checkpoint d84d218 และยังไม่ได้ commit เปลี่ยนเฉพาะส่วนส่งออกและข้อความหน้าจอ ไม่เปลี่ยนการค้นหา ดู [รายงานการแก้ส่งออก](audit/best-trial-export-2026-09-10/REPORT.md)

งานต่อเนื่องจากแชทวันที่ 9 กันยายน 2569 ให้ถือสถานะในไฟล์นี้เป็นปัจจุบัน ประวัติการทดลอง/คำสั่งเดิมเก็บครบใน [handoff-history](audit/handoff-history-2026-09-09.md) หัวข้อ “Current” ในประวัตินั้นเป็นสถานะในอดีต อ่านเมื่อจำเป็นต้องตรวจที่มาของการตัดสินใจ

## เริ่มงานในแชทใหม่

1. ใช้ `C:\reserch 69\RC_Wall_Fix` โดยตรง ไม่สร้าง worktree หรือย้ายไปอีกสำเนา
2. อ่านไฟล์นี้และ AGENTS.md ถ้ามี ตรวจ git status, branch, remote และ `RC_RT_HCA_v2.vbp` ก่อนแก้ ปัจจุบันไม่มี AGENTS.md ใน root
3. โปรเจกต์ที่ผู้ใช้รันคือ `RC_RT_HCA_v2.vbp` เปิดใหม่ใน VB6 แล้ว F5 ผู้ใช้ต้องการรันทดลองเอง
4. ถ้ายังไม่มีคำสั่งงานใหม่ ให้สรุปสถานะสั้น ๆ แล้วรอผู้ใช้ ไม่เริ่มค้นหาวิธี BA หรือรันทดลองต่อเอง

## สถานะโค้ดที่ติดตั้งแล้ว

- BA ใช้ directed-center candidate วันที่ 10 กันยายน 2569 ตาม hash ด้านบน ติดตั้งตามคำสั่งล่าสุดแล้ว เพิ่มข้อเสนอแบ่งครึ่งไปทางขนาดต่ำกว่าใน GenerateNeighbor เมื่อ EvaluationCount Mod 4 = 2; tt/tb/TBase/Base เลือกกึ่งกลางจากขอบล่างเดิมถึงค่าปัจจุบัน คง toe และเลื่อนคู่เหล็กทั้งสามไปอันดับกึ่งกลางทางน้ำหนักต่ำกว่าใน catalogue เดิม
- HCA ใช้ตัวค้นหาเดิม: เริ่มแบบเดิม สุ่มปรับจากสถานะปัจจุบันตามกฎ HCA เดิม ยอมรับแบบที่ผ่านและราคาดีขึ้น เก็บ best และทำงานครบงบ
- BA ยังมีข้อเสนอจุดกึ่งกลางร่วมของ tb/TBase/Base ตามตารางเดิม สำหรับ neighbor ที่ไม่เข้าเงื่อนไข directed-center ใช้การสุ่มเดิม: มิติทั้งห้าสุ่มอิสระ P(คงค่า)=2/3, P(+1)=P(-1)=1/6; คู่ขนาด–ระยะเหล็กทั้งสามส่วนใช้ตัวเลือกข้างเคียงและโอกาสคงคู่ 1/2
- จุดกึ่งกลางเลือกค่าที่ใกล้ที่สุดจาก domain เดิม หากห่างเท่ากันเลือกค่าต่ำกว่า ไม่เพิ่มขนาดใหม่ Midpoint ทั้งสามเปลี่ยนพร้อมกัน พร้อม repair tt≤tb เดิม และถูกนับงบทุกครั้ง; ถ้าไม่ผ่าน/ไม่ดีขึ้น คืนสถานะครบ 11 ค่า
- ช่วงค้นหาระหว่าง midpoint มี 20,40,60,... ข้อเสนอ ปรับปลายช่วงจาก incumbent และราคาอ้างอิงเมื่อจบช่วง Bounds นี้ใช้หาจุดกึ่งกลาง; ข้อเสนอสุ่มยังใช้ domain โจทย์เดิม ไม่ใช่บังคับตัด domain ครึ่งถาวร จึงต้องอธิบายวิธีตามโค้ดจริง
- การเปรียบเทียบเป็น BA ทั้งวิธีเทียบ HCA ทั้งวิธี ไม่ได้แยกพิสูจน์ประโยชน์ของ bisection เพียงกลไกเดียว Comments บางแห่งยังเป็นคำอธิบายจากการพัฒนารุ่นเก่า อย่าใช้ comment ที่ว่า “both buttons use HCA” เพื่อตัดสินว่า BA ปัจจุบันเรียก HCA
- ช่อง seed และการตั้ง seed ซ้ำถูกนำออกแล้ว Randomize ครั้งแรกจากระบบและใช้ stream ต่อไปตามธรรมชาติ หน้าต่างหลักเปิดกลางหน้าจอ

## ข้อกำหนดที่ต้องรักษา

- รักษาสูตร หน่วย ราคา domain และชุดตรวจเดิม ไม่เพิ่มขอบเขตวิศวกรรมหรือเปลี่ยนอัลกอริทึมเอง การแก้เพิ่มเติมต้องตรงคำสั่งผู้ใช้
- แบบที่ยอมรับต้องผ่านชุดตรวจที่เปิดใช้อยู่ รวม overturning, sliding, bearing ทั้งสามข้อ และการตรวจหน้าตัด/เหล็กที่กำหนด ไม่ลดเกณฑ์เพื่อให้ BA ชนะ
- เกณฑ์กราฟล่าสุดแทนเป้าหมายเดิมประมาณ 10%: BA ต้องราคาไม่สูงกว่าและรอบน้อยกว่า HCA ใน Trial ที่เลือกตามเกณฑ์เดียวกันทั้งสองวิธีครบทุกความสูง ราคาเท่ากันผ่านได้ แต่รอบเท่ากันไม่ผ่าน ส่วนสถิติยังต้องพิจารณาครบทุก Trial รวมครั้งที่ไม่ถึงราคาอ้างอิงและรายงาน SD ไม่เลือกทิ้งผล
- รักษา `result_csv` ทั้งไฟล์ที่มีอยู่และรายการที่ผู้ใช้ลบไปแล้ว ห้ามเขียน/ลบ/คืนไฟล์/จัดระเบียบหรือ stage ผลเหล่านี้เอง ปัจจุบันมี tracked deletions และ untracked archive ค้างอยู่โดยตั้งใจไม่รวมใน checkpoint
- ข้อห้ามรัน optimizer เดิมได้รับการยกเว้นด้วยคำสั่งล่าสุด “ทดลองเลย” และดำเนินการครบหนึ่งชุด 180 Trial ใน audit แล้ว รายงานผลตามจริง ไม่รันซ้ำจนได้ชุดที่ชนะ การเปลี่ยนสูตร หน่วย domain หรือเกณฑ์ตรวจยังไม่อยู่ในขอบเขตที่อนุญาต
- แก้ VB6 แบบรักษา encoding เดิม/CRLF ไม่มี BOM; อย่าเขียน .frm/.bas ทั้งไฟล์เป็น UTF-8

## CSV ปัจจุบันตามตัวอย่างของผู้ใช้

ไฟล์หลักอยู่ใน `C:\reserch 69\RC_Wall_Fix\result_csv` ชื่อใช้ H และ f′c ที่รันจริง ตัวอย่าง H3/fc240:

| ไฟล์ | คอลัมน์ | ขอบเขตข้อมูล |
|---|---|---|
| accept-BA-H3-240.csv | No.,Rejected,Passed,Passed and Better value | ประวัติครบของ Trial ที่ดีที่สุดของ BA |
| accept-HCA-H3-240.csv | No.,Rejected,Passed,Passed and Better value | ประวัติครบของ Trial ที่ดีที่สุดของ HCA |
| loopPrice-BA-H3-240.csv | No.,Loop,BestPrice | สรุปทุก Trial |
| loopPrice-HCA-H3-240.csv | No.,Loop,BestPrice | สรุปทุก Trial |
| selectedTrial-BA-H3-240.csv | No.,Loop,BestPrice,Trials,Status | ระบุ Trial ของ accept ที่เลือก พร้อมจำนวน Trial ทั้งชุด |
| selectedTrial-HCA-H3-240.csv | No.,Loop,BestPrice,Trials,Status | ระบุ Trial ของ accept ที่เลือก พร้อมจำนวน Trial ทั้งชุด |

ราคา 2 ตำแหน่ง No. ใน loopPrice เริ่ม1 และมีหนึ่งแถวต่อ Trial กรณีไม่มีคำตอบผ่านยังมีแถวและราคาว่าง ไม่มีการสร้าง archive ใหม่ ไม่มีโฟลเดอร์ราย Trial หรือ acceptRuns; modTrialExport ถูกนำออกจากโปรเจกต์แล้ว การเลือกราคาใช้ค่าจริงก่อนปัดเศษ ตรงกับเกณฑ์เลือก Trial บนหน้าจอ

ระหว่างรัน FinishSearch ยังบันทึก accept ของแต่ละ Trial ตามเดิม เมื่อจบชุด SaveLoopPriceCSV จะเขียน accept ของ Trial ที่เลือกแทนเฉพาะเมื่อผู้ชนะไม่ใช่ Trial สุดท้าย โดยทับชื่อเดิมโดยตรงตามคำสั่งล่าสุด selectedTrial มี Status=SELECTED; ถ้าไม่มี Trial ผ่านเลยให้ No./Loop/BestPrice ว่าง Status=NO_SOLUTION และเก็บ accept ของ Trial สุดท้ายสำหรับตรวจสาเหตุ ไม่สร้างผู้ชนะขึ้นเอง การเปลี่ยนนี้มีผลเมื่อผู้ใช้รันโปรแกรมครั้งต่อไปเท่านั้น ไฟล์ผลเดิมยังไม่ได้เขียนทับ

สำหรับกราฟลู่เข้า ใช้ No. และราคาจาก Passed and Better value ของ accept เป็นเส้นขั้นบันได best-so-far ของ Trial ที่เลือก ไม่ใช้ราคาของข้อเสนอที่ Rejected หรือ Passed แต่ไม่ดีกว่าเป็น incumbent และไม่ประกอบหลาย Trial เป็นเส้นเดียว กราฟนี้เป็น best-trial illustration ไม่ใช้แทนสถิติครบ 30 ครั้ง

ตั้ง5,000 หมายถึง5,000การประเมินรวมค่าเริ่มต้น: accept No.0–4,999 ตัวอย่างเก่ามี0–5,000 แต่ไม่ได้เพิ่มงบหรือเติมแถวตามตัวอย่างเก่า CSV Loop เริ่ม0 ส่วนเลข best evaluation บนหน้าจอเริ่ม1 ต้องแปลงฐานก่อนเทียบ อย่าแก้เลขให้ดูเหมือนเร็วขึ้น

แหล่งตัวอย่างอ่านอย่างเดียว: `D:\rc-rt-optimize-v2\vb6_samples` รายงานล่าสุด [sample-csv-export/REPORT.md](audit/sample-csv-export/REPORT.md)

## หลักฐานผลพัฒนาที่มีอยู่

แต่ละ trial ใช้5,000การประเมินจริง ทั้งสองวิธีทำครบงบ ตัวชี้วัด effort เป็นจำนวนประเมินถึงราคาอ้างอิงร่วมครั้งแรก; ถ้าไม่ถึงใช้ค่าจำกัด5,000 พร้อมระบุว่าไม่ถึง ไม่ใช่อ้างว่าพบที่รอบ5,000 ราคาอ้างอิงเป็นค่าดีที่สุดที่พบ ไม่ใช่ optimum ที่พิสูจน์แล้ว

| กรณี | ถึงราคา BA/HCA | effort เฉลี่ย BA/HCA | SD effort BA/HCA | ลด effort |
|---|---|---|---|---|
| H3/fc240 | 20/20 : 20/20 | 271.40 : 403.25 | 141.60 : 202.69 | 32.70% |
| H4/fc280 | 20/20 : 19/20 | 474.00 : 1953.10 | 298.33 : 1356.85 | 75.73% |
| H5/fc320 | 11/20 : 4/20 | 2719.85 : 4346.30 | 2141.78 : 1379.23 | 37.42% |

H3 เป็น10+10ต่อวิธีสองชุดที่คง candidate เดียวกัน H4/H5 เป็น20ต่อวิธีในแต่ละกรณี SD effort ของ BA ที่ H5 สูงกว่า HCA ต้องรายงานตามจริง ทุกคำตอบสุดท้ายผ่านชุดตรวจ แต่ไม่ใช่ทุก trial ถึงราคาอ้างอิง ไม่มีการคำนวณ p-value สำหรับชุดใหม่30ครั้ง

อ่านตามงานที่จะทำ:
- วิเคราะห์ที่มาหรือ logic BA: [H3 protocol/report](audit/ba-geometry-hold/REPORT.md) และ candidate-modBA.bas ในโฟลเดอร์เดียวกัน
- วิเคราะห์ H4: [รายงาน H4](audit/ba-geometry-hold-h4-fc280-20/REPORT.md)
- วิเคราะห์ H5: [รายงาน H5](audit/ba-geometry-hold-h5-fc320-20/REPORT.md)
- ตรวจการติดตั้ง: [installation report](audit/ba-geometry-hold-installation/REPORT.md) หมายเหตุ whole-file hash เก่าต่างได้จากการเปลี่ยนชื่อ CSV ล่าสุด; search core ยังตรง candidate
- ตรวจส่งออกปัจจุบัน: [verification.json](audit/sample-csv-export/verification.json) ผ่าน27 native +27 independent checks คอมไพล์ root sources ผ่าน ไม่มี optimizer call ใน probe ตัวอย่างทั้ง4ตรงทุกไบต์และ result_csv ทั้ง326ไฟล์ไม่เปลี่ยน

## งานค้างที่ต้องทำต่อเมื่อผู้ใช้ส่งข้อมูล/สั่ง

1. รอผลผู้ใช้รัน VB6 รุ่นล่าสุด 30ครั้งต่อวิธีภายใต้โจทย์เดียวกัน ไม่เลือก archived รุ่นเก่ามาแทน
2. ผู้ใช้กำหนด one-sided paired t-test ที่ α=0.05 จับคู่ตามลำดับ No.1–30 จาก loopPrice ของ BA/HCA เพราะ accept หลักมีเพียงหนึ่ง Trial (ไฟล์เดิมเป็น Trial สุดท้าย รุ่นแก้ใหม่เป็น Trial ที่ดีที่สุด) ตรวจ H/fc/งบและจำนวนคู่จริงก่อนคำนวณ ระบุ run-order pairing ตามจริง ไม่เรียก shared-seed pairs ไม่เรียงผลตามราคา/ความเร็วเพื่อเลือกคู่
3. ระบุตัวชี้วัด ทิศทางสมมติฐาน และกรณีไม่ถึงเป้าหมายให้ชัด ประเมินทั้งชุด ไม่สรุปความเร็วจาก first-best ที่คนละราคาเพียงอย่างเดียว และไม่ใช้150000แถว trace เป็น150000ตัวอย่างอิสระ
4. บทความต้นฉบับ `C:\reserch 69\การออกแบบกำแพงกันดินคอนกรีตเสริมเหล็ก edit22.docx` ตรวจแบบอ่านอย่างเดียวแล้ว15หน้า ยังไม่แก้ไฟล์ ดู [REVIEW.md](audit/manuscript-edit22-review/REVIEW.md) ก่อนแก้ เมื่อผู้ใช้สั่งให้แก้ต้องไม่เกิน15หน้าและตรวจ layout ใหม่

## Git checkpoint และไฟล์ที่คงในเครื่อง

Repo `https://github.com/dohcivil-ui/RC_Wall_Fix.git`, branch `main`; ผู้ใช้สั่ง commit → pull → push ใช้ commit subject ตรงตัวว่า `version revise BA pass with resersh` ตรวจ hash/remote ปัจจุบันด้วย git log/status ไม่อนุมานว่า working tree ต้องสะอาด

Checkpoint รวม root source ที่แก้, handoff, คำสั่งเปิดแชท และหลักฐานสรุป/ค่ารายTrialที่เกี่ยวข้อง ส่วน result_csv, raw evaluation traces ขนาดใหญ่, EXE, PDF/ภาพ render และการทดลองรุ่นเก่าบางส่วนยังเป็นไฟล์ local จึงอาจมี untracked files หลัง push ไม่มีการ clean/stash/reset หรือเปลี่ยนผลเหล่านี้เพื่อทำให้ status สะอาด

ข้อความพร้อมใช้เปิดแชทใหม่อยู่ใน [NEXT_CHAT_PROMPT.md](NEXT_CHAT_PROMPT.md)
