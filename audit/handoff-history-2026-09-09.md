# RC_Wall_Fix handoff — 2026-09-09

## Current export contract: match the user's four CSV examples

- Latest user explicitly requested exports matching D:\rc-rt-optimize-v2\vb6_samples\accept-{BA,HCA}-H3-240.csv and loopPrice-{BA,HCA}-H3-240.csv. This supersedes the earlier acceptRuns/23-column plan below. User will run the root VBP themselves. No optimizer/30-trial experiment authorized in this export task.
- Root modBA.bas/modHillClimbing.bas each change only two output filename stems to include actual H and currentMaterial.fc: accept-METHOD-H3-240.csv and loopPrice-METHOD-H3-240.csv. Original four-column accept and three-column loopPrice headers, two-decimal prices, category placement and zero-based Loop remain unchanged. accept is the LAST trial trace; loopPrice summarizes ALL trials, No.1..N. Use loopPrice No. for future user-directed run-order pairing; one accept trace is not30 statistical samples.
- Removed all Form1 Begin/Append/display acceptRuns hooks and the modTrialExport reference from root VBP. Removed that previously-created root module after backing it up under audit/sample-csv-export/before. Retained UI error recovery and changed its displayed paths to actual accept/loopPrice files. No extra combined export or per-trial directory. Existing archive preservation stays; redundant final accept write stays removed.
- Sample accept length is5001 rows No.0..5000. DO NOT alter current initial-inclusive evaluation budget to copy this old length: configured5000 remains5000 evaluations and export No.0..4999. No fabricated/duplicated row or extra evaluation. Screen first-best is one-based, CSV Loop is zero-based as before. Search/formulas/units/domains/randomness/checks unchanged.
- Validation audit/sample-csv-export: actual-source VB6 compilePASS; native fixture replay27checksPASS reproduces ALL FOUR sample files byte-for-byte using existing data only, no optimizer calls. H4/fc280 dynamic filename and no-solution checksPASS. Independent27checksPASS; all326 user result_csv files/inventory and four source samples unchanged. Root EXE unchanged; compiled audit binary only. No commit/push.
- User deliverable is root RC_RT_HCA_v2.vbp opened fresh in VB6. Report audit/sample-csv-export/REPORT.md; verification.json records current hashes. Earlier export audit binaries/source hashes describe historical states and must not be used as latest or repaired by reverting this change. No p-values computed; manuscript original still untouched and15-page cap remains.

## Current ready build: all-trial accept export; user will run the experiments

- Latest instruction: finish the code first; user will run themselves. Do NOT run optimizer/30 trials or select archived data for statistics now. User requests run-order matching from the new BA/HCA accept files, 30 trials per method, one-sided paired t-test, alpha=0.05. Pending actual data; no p-values computed. Do not sort outcomes to manufacture pairs or claim shared-seed pairing. No seed added.
- Output-only fix installed: new modTrialExport.bas, added to root RC_RT_HCA_v2.vbp; Form1 initializes/appends/displays combined accept and restores buttons/mouse on errors. A flat unique acceptRuns-METHOD-Hx-fcy-TIMESTAMP.csv is written incrementally after completed trials, with original four columns plus 19 metadata columns (23 total). Includes Trial, scenario, budget, first-best evaluation, final price, status and requested trial count. No per-trial folders.
- BestEvaluation is one-based native evaluation count; original No. and loopPrice Loop remain zero-based. BestPriceExact is formatted to TEN decimal places with existing CsvNumber, not raw Double bits. Final-trial metadata repeats across each trial's trace. Preserve no-solution outcomes (zero best evaluation, blank best price); 30x5000 trace rows represent 30 statistical units, not 150000 independent observations.
- modBA.bas and modHillClimbing.bas each remove ONLY the redundant accept write inside SaveLoopPriceCSV. Their entire search code is byte-identical to the pre-export state; pre-export BA equals the tested geometry-hold candidate. Prior whole-file hash statements below are historical and superseded for these output edits. All engineering/shared sources, formulas, units, checks, random draws and domains unchanged.
- Latest ready EXE: audit/trial-accept-export/production/RC_RT_HCA_v2.exe (266240 bytes), compiled from actual root sources with Startup Form1. Open root VBP fresh or use THIS binary, replacing the earlier installation audit EXE as the latest deliverable. Root EXE not created/replaced.
- Validation: production/probe VB6 builds succeeded; 32 native SYNTHETIC export checks and 63 independent CSV/metadata/source/protected-file checks PASS. Probe has zero optimizer calls/runs and uses isolated audit output. GUI error recovery source-reviewed/compiled, not exercised through an optimizer GUI run. User result_csv inventory and all326 file hashes unchanged; no commit/push. Evidence audit/trial-accept-export/REPORT.md and verification.json; backups in before/.
- Manuscript review remains read-only for original edit22.docx (15 pages); do not exceed15 when subsequently editing. New 30-run results are pending and must not be replaced by old manuscript statistics or exploratory20-run datasets. The review note records latest user-directed run-order pairing and self-run plan.

## Installed now: tested geometry-hold BA after authorized H5/fc320 comparison

- User explicitly authorized H5/fc32020trials each and applying code if overall results remain good. H5 used the SAME candidate as H3/H4, one40-runbatch; no tuning/retries. Fullresults audit/ba-geometry-hold-h5-fc320-20/REPORT.md; actualbatch audit/parameter-trials/h5-fc320-20. All40finals valid,200000vectors and172nativechecks pass before installation.
- H5 reference8538.41892 observed across this batch. BA11/20 vsHCA4/20 reach it; all-run capped effort2719.85 vs4346.30, reduction37.42%. Finalprice mean8564.4211576/8617.0906033 and SD40.24/115.62. IMPORTANT capped-effort SD BA2141.78 is HIGHER thanHCA1379.23; report it. Success-only means854.27/1731.50 and SD456.18/807.70 are conditional, not all-runs substitutes. Overall attainment/effort supports user's~10% target despite imperfectattainment and higher cappedSD; no universal/statistical claim.
- Root modBA.bas is NOW byte-identical to audit/ba-geometry-hold/candidate-modBA.bas and executed H5snapshot. It no longer delegates to HCA baseline. Form1.frm changes ONLY two stale progress strings to BA / BA - Trial. HCA/shared/otherrootmodules, engineeringformulas,units,prices,domains,checks,VBP and CSV behavior remain unchanged. BA all5geometry+3pair independentdraws remain simultaneous, midpointjointtb/TBase/Base, fullbudget and exactaccept/restore as tested.
- Installation audit/ba-geometry-hold-installation: before/modBA.bas and before/Form1.frm preserve pre-install bytes; installation-decision.json records authorization/metrics; installation-verification.json32checksPASS confirms scope,publicsignatures,sourcehashes/protectedoutput and productioncompile. RootVBP/FRX/EXE inventory and userresult_csv inventory+bytes unchanged. Changed rootsourcefiles this installation are ONLY modBA.bas and Form1.frm (plus thishandoff).
- Compiled actual root sources via audit/ba-geometry-hold-installation/production/Production.vbp, StartupForm1,no probe. Build succeeded; readybinary production/RC_RT_HCA_v2.exe. Open root RC_RT_HCA_v2.vbp fresh to load installedsource. No extraoptimizer afterinstallation, no30trialbatch,rootEXEreplacement,commit/push or backgroundwork. Earlier "candidate not installed"/"BA=HCAcontrol" sections below describe historical states and are superseded.
- Historical experiment verifiers that require ALL pre-installroot hashes are pre-install evidence; they may intentionally fail after this authorizedsource change. Use installation-verification for currentstate; do not revert installedBA to satisfy oldbaseline assertions. H5 GUI320 is index4, runtimeactualH/fcchecked everytrial; obsolete sharedfcArray indexing must not be used to select GUIvalues.

## Latest conditional-install test: H5 / fc320, twenty trials per method

- User explicitly requested H5m/fc320 and20trials EACH. One actualUI BA20x5000+originalHCA20x5000, all40outcomes retained. SAME geometry-holdcandidate bytes as H3; no tuning/retries/replication/newvariant. Protocol/report audit/ba-geometry-hold-h5-fc320-20; fullbatch audit/parameter-trials/h5-fc320-20. AllotherGUIinputs same asH3. NativeH/fcchecked everytrial.
- Symmetric reference is best observed valid newbatch price8538.41892, notH3target or provenoptimum. BA11/20 vsHCA4/20 reach it. Meanallrunseffort BA2,719.85/HCA4,346.30; reduction37.42%; sampleSDallrunseffort BA2,141.78/HCA1,379.23. Meanvalidfinalcost BA8,564.421158/HCA8,617.090603. Reportallmissesandconditionalmeans; noforcedwinorcompositeweights.
- Native40trials/0failures;172CSV/UI/input/source/price/safetychecks and200000independentfullvectorchecksPASS, includingH5domains,midpoints,exactDoubleaccept/restore/firstbest. OutputmetadataIntegerfcconvertedCDbl inAUDITprobeonly to matchexistingCsvNumber signature, noformula/sourcechange. Root11source/VBP/EXE/FRX/userresult_csv hashes/inventoryunchanged.
- This is the pre-install verification state. User explicitly authorized applying the exact candidate if results support overall benefit around10%; see a newer installation entry for final installed status. No30trials,rootEXEreplacement,commit/push,oradditionaloptimizerwork. Comparecasesseparately; H3successdoesnotestablishH5superiority. Read REPORT.md forresultinterpretationandall40values.

## Latest user-requested test: H4 / fc280, twenty trials per method (unchanged candidate)

- User explicitly requested H4m/fc280 and20trials EACH. One actualUI BA20x5000+originalHCA20x5000, all40outcomes retained. SAME geometry-holdcandidate bytes as H3; no tuning/retries/replication/newvariant. Protocol/report audit/ba-geometry-hold-h4-fc280-20; fullbatch audit/parameter-trials/h4-fc280-20. AllotherGUIinputs same asH3. NativeH/fcchecked everytrial.
- Symmetric reference is best observed valid newbatch price4985.56464, notH3target or provenoptimum. BA20/20 vsHCA19/20 reach it. Meanallrunseffort BA474.00/HCA1,953.10; reduction75.73%; sampleSDallrunseffort BA298.33/HCA1,356.85. Meanvalidfinalcost BA4,985.564640/HCA4,989.725548. Reportallmissesandconditionalmeans; noforcedwinorcompositeweights.
- Native40trials/0failures;172CSV/UI/input/source/price/safetychecks and200000independentfullvectorchecksPASS, includingH4domains,midpoints,exactDoubleaccept/restore/firstbest. OutputmetadataIntegerfcconvertedCDbl inAUDITprobeonly to matchexistingCsvNumber signature, noformula/sourcechange. Root11source/VBP/EXE/FRX/userresult_csv hashes/inventoryunchanged.
- CandidateNOTinstalled; rootBAremainsBA=HCAcontrol. No30trials,rootEXEreplacement,commit/push,oradditionaloptimizerwork. Comparecasesseparately; H3successdoesnotestablishH4superiority. Read REPORT.md forresultinterpretationandall40values.

## Latest development: independent geometry hold with original HCA comparison (audit only)

- Positive exploratory evidence in this H3/fc240 case: stage1 mean reduction38.80%, unchanged-code replication25.92%; both stages alltargetattainment equal. Aggregate32.70% fewer evaluations and30.14% lower SD. Meets the ~10% development target in these samples, not universal or statistical proof. Independent aggregate audit/ba-geometry-hold/independent-aggregate.json agrees; all200000vectors/40trials verified. Rootprogram remains BA=HCAcontrol; tested replacement is candidate-modBA.bas in audit/ba-geometry-hold.
- User authorized further development toward ~10% overalladvantage, not perfecttrials. Frozen onecandidate audit/ba-geometry-hold/PROTOCOL.md; REPORT.md has ALL outcomes. Stages completed: geometry-hold-10, geometry-hold-replication-10; eachBA10x5000+originalHCA10x5000. Stage2 was prespecified conditional fresh replication; no tuning betweenstages or thirdbatch. Rootcandidate NOT installed.
- BA changes ONLY five independentgeometrydraws to P0=2/3,P±1=1/6 viaRand1..6, retaining lazy-pair half-stay3memberdraws, all8drawsbeforeoneevaluation, originaldomains/checks/formulas/prices/units, jointtb/TBase/Basemidpoints/quotas/acceptance/fullbudget. No fixedmembers/height-targetspecialcases. HCA/sharedexactroot. Diagnosis showed60targetsteelproposals spoiledbygeometry; evidence audit/ba-geometry-delay-diagnosis.
- Aggregate descriptive outcome: BA20/20 vsHCA20/20 atknown2942.34192; meanallrunseffort 271.40/403.25, reduction32.70%; sampleSD 141.60/202.69. Stage-specific performance-summary.json must accompanyaggregate. Effort uses full5000budget for misses, not fictionaltargetat5000. SDsupportive; do not imposeperfection or inventcompositeweights. No universal/statisticalsuperiorityclaim.
- Nativebeforecatchesoldticketpolicy; after exhaustive7776geometryvectors+allpairtickets+boundedmechanismPASS. Eachstage91nativechecks and100000independentvectorchecksPASS; exact source/rootVBP/EXE/FRX/result_csv hashes unchanged. No30trialbatch,rootEXEreplacement,commit/push or moreoptimizerwork. RootBAstillBA=HCAcontrol.

## Latest user clarification: overall BA advantage, not perfection in every trial

- Latest quantitative guidance: an overall advantage of approximately 10% or more is an acceptable development target (user: "ประมาณ 10%ขึ้นไปก็โอเค"). Replace the earlier unspecified improvement magnitude with this target. Assess the complete sample using attainment, target-first evaluations and SD together; retain misses. Report each metric and its comparison basis separately. The user has not defined weights for a composite score, required each metric to improve by 10%, or required every run to win; do not invent those conditions or count a favorable metric alone as proof of overall advantage.
- User clarified the objective: BA should find comparable-quality answers more often and faster than HCA overall. BA need not reach the target in 10/10 trials or beat HCA in every individual run. Do not reject a promising candidate solely because one or more trials miss the target. This supersedes any earlier interpretation requiring perfect attainment or a per-run win.
- Compare unchanged original HCA and BA at the same inputs, engineering checks, target price and actual evaluation budget. Report attainment together with target-first evaluation counts across the full trial set; keep nonattaining runs visible. A successful-runs-only average is descriptive, not sufficient evidence of overall advantage. Attainment versus evaluation budget can show both aspects without assigning an invented aggregate score.
- SD remains useful supporting information, not a newly imposed mandatory win condition. If attainment and speed trade off, present both rather than silently inventing weights or declaring victory. Keep original data and comparator identity explicit. This clarification records the development objective; it does not authorize a 30-trial batch or changes to engineering formulas, units, domains, or user result_csv.

## Latest development: half-stay coupled steel pairs versus original root HCA (audit only)

- Objective remains unmet: both10/10target, but BA mean755.3 and SD461.54 are above originalHCA mean554 and SD459.83. Firstfeasible BA18.9/HCA5.1; neighborvalidity59.27%/45.53%. More feasible proposals did not imply earlier target. Observed self49.977%,6281neighbors change all3steel pairs; no grouping introduced. Preserve this negative speed result.
- User authorized continued development. One new frozen condition, actualUI BA10x5000+HCA10x5000 atH3/fc240, natural RNG; no performance reruns/tuning/extra condition. Protocol/report audit/ba-lazy-pairs; recorded batch audit/ten-trials-h3/lazy-pairs-10. Candidate NOT installed.
- Prior failing BA4 trace shows near-target proposal rarity: after969,3205neighbors included13target steel triples, none at targetgeometry. Pair bridge exists; not an acceptance/restore bug. Diagnosis preserved audit/ba-coupled-next-diagnosis.
- ONLY PairNeighbor changes in BA from coupled-pairs: independent Pstay=1/2 per member; otherhalf uniform among original union NONSELF pairs. Single Rand ticket1..2(k-1); all3 pairs plus5geometry draws before full evaluation. Midpoint/phase/globaldomains/formulas/prices/checks untouched. For prior k7 one-deficit state direct fulltarget probability1/243 vs1/1736, not whole-run speed guarantee.
- Comparator is ORIGINAL ROOT HCA/shared, byte-identical before audit trace/output hooks. It retains grouped/wider/absolute sampling and tt repair. Do not confuse with preceding adjacent-one-level HCA. Shared differs from prior adjacent candidate only sampler+policylabel; BA private sampler unaffected. This compares whole methods, not bisection alone.
- Outcome BA10/10,HCA10/10 at known target2942.34192. Successful target-first mean BA755.30/HCA554.00; sampleSD BA461.54/HCA459.83. Finalmean BA2942.34192/HCA2942.34192. Keep failures and conditional means explicit; report has all20results. No statistical/universal superiority claim or automatic promotion.
- Native before catches wrong ticket contract; after mechanism passes. Actual20trials/0failures,91nativechecks and independent100000-vector validationPASS. Rootsource/VBP/EXE/FRX/result_csv inventory+hashes unchanged. Root modBA remains BA=HCAcontrol. No30trialbatch,rootEXEreplacement,commit/push,or further optimizer scheduled.

## Latest test: admissible BA midpoint + coupled steel pairs (audit only; 9/10 target)

- User authorized "ลองเลย" for one new condition: actual UI BA10x5000 + HCA10x5000 at H3/fc240, natural randomness. No reruns/tuning/additional condition. Read audit/ba-coupled-pairs/PROTOCOL.md and REPORT.md; complete batch audit/ten-trials-h3/coupled-pairs-10. Candidate NOT installed.
- HCA/shared byte-identical to prior adjacent-shared audit candidates: simultaneous full11-index +/-1 local moves. This is NOT the original grouped root HCA. BA geometry remains one-level; midpoint policy retains simultaneous tb/TBase/Base,20/40/... phases,global domain and full budget.
- BA now accepts midpoint only if fullyvalid and cheaper (or firstvalid), otherwise restores all11 indices and unchanged incumbentcost/validity. BA samples each member's DB/SP jointly from a deduplicated union of original one-level DB/SP pairs and adjacent weight-ranked pairs among all20existing options. All3 member pairs sampled alongside geometry before one evaluation. This preserves old local transitions and adds DB16@0.25->DB12@0.15; no formula/check/unit/domain/price changes.
- Outcome: BA9/10 at known observed target2942.34192, HCA0/10. BA target-first197,591,1608,missing,2686,257,2993,795,1570,1814; successful-only mean1390.11,SD1012.93. BA trial4 final2947.317408. Mean final BA2942.8394688/SD1.573387455; HCA2958.689952/SD13.56135436. HCA has no same-target time comparator. All20finals valid. Do not claim completed reliability, statistical superiority, universal speed gain, or superiority over original root HCA.
- Native rejected-midpoint regression fails before;73693 checks pass after. Actual20trials/0failures,91 output/source/safety checks and independent100000-vector exactDouble reconstruction PASS. BA220midpoints:21accepted/199rejected; all16invalid rejected. First-feasible meanBA13.9/HCA23.3. Added weight-rank-only pair transitions exercised21291/149310. Each BA5000=1initial+22midpoint+4977neighbor; no rounded/exact first-best discrepancy this batch.
- This tests TWO BA changes together, not a bisection-only ablation or exact paper reproduction. Full details and limitations in report. Root11 source hashes and user result_csv inventory/bytes unchanged (final-verification.json). Root modBA remains BA=HCA control; no root EXE replacement,30-trialbatch,commit/push,or further optimizer work. Keep all results; no automatic promotion.

## Latest test: both methods share article-style adjacent full-vector sampling (audit only)

- User's Thai-layout " ]v'g]p " means "ลองเลย", authorizing the proposed matched one-level trial. One actual UI BA10x5000 + HCA10x5000 atH3/fc240, natural randomness, no post-result tuning/retries/extra condition. Read audit/ba-adjacent-shared/PROTOCOL.md and REPORT.md. No candidate installed.
- Both audit methods use identical11-variable current-index+Rand(-1,1), no member grouping or absolute bar sampling. Neighbor and conservative initializer byte-identical. Remove multi-level tt repair from BOTH neighbors; unchanged GeometryOK rejects tt>tb. BA alone retains counted joint midpoint/tt repair and20/40/...phase policy from column candidate. This compares the whole center mechanism, not a3-variable-only ablation.
- Outcome: BA0/10 at observed target2942.34192; HCA2/10 (1726,1608). BA final mean2962.5518784, range2945.421984..2978.118048; HCA mean2956.6523712, range2942.34192..2974.090272. All20finals pass configured checks and have0.20/0.30/1.50geometry. BA joint-geometry first-feasible mean927.4 vsHCA247.6. No speed/stability success or significance claimed.
- Verification: old sampler fails native local-offset contract; after69254PASS. Actual20runs/0failures,91CSV/UI/source/safety/exact-best checks and independent100000-vector reconstructionPASS. Snapshot candidate sources/initializer/neighbor parity/protectedrootfiles verified. Fixed trace-only ByRef compile error BEFORE any optimization; log preserved.
- Important new precision evidence: BA1 native first-best3563 vs firstsame10decimalcost2162; HCA2 1881vs1230; HCA8 1653vs1327. Recost12826 already-recorded feasible designs with unchanged native formulas, NO optimization/randomness, recovers exactDouble bits and verifies every nativebest/acceptance. Do not silently substitute rounded first-price indices for nativeones, nor change price comparison/tolerance without methodological work. Target attainment0/10 vs2/10 is unaffected.
- Results audit/ten-trials-h3/adjacent-shared-10; candidate audit/ba-adjacent-shared. Root BA=HCA control remains installed; rootHCA/shared unchanged. Noresult_csv writes,30trialbatch,rootEXE replacement,commit/push or active optimizer. Do not promote this unsuccessful method automatically.

## Latest test: supplied-column midpoint/HCA adaptation (audit only; failed target)

- User authorized testing the column-inspired method just analyzed. One audit-only candidate, one actual UI10x5000 per method at H3/fc240. Protocol fixed before batch; no retries/tuning/second candidate. Read audit/ba-column-midpoint/PROTOCOL.md and REPORT.md. Root BA remains BA=HCA; this candidate is NOT promoted.
- Candidate relocates tb/TBase/Base simultaneously, performs20/40/60/... full-vector neighbors using unchanged global wall domains, and updates center endpoints from checked local cost/reference. Nearest existing dimensions with lower ties; evaluates/counts every midpoint, keeps independent global best, orders crossed center endpoints, exact budget. BA applies all11 raw variables; HCA keeps its member grouping. Thus comparison includes grouping policy as well as midpoint scheduling, and is not a bisection-only ablation or reproduction of the column paper.
- Outcome: BA3/10 reach observed common target2942.34192 (first2327,608,3041);7failures end2947.317408..2985.58128. HCA10/10, first181,541,308,255,441,472,427,272,119,344 (mean336; SD134.35). BA conditional successful-only mean1992 is not an all-trial speed measure. BA mean final price2958.6188736. All20 final designs pass configured checks; all have tb/TBase/Base0.20/0.30/1.50. Remaining-variable search is an investigation lead, not proven isolated cause.
- Evidence: native mechanism20321PASS (before catches missing midpoint); actual20runs/0failures;100000 evaluation/CSV/UI/source/protected-output verification91PASS. Each actual BA trial has1initial+22midpoint+4977neighbor=5000. audit/ba-column-midpoint/final-verification.json confirms root source and user result_csv hashes unchanged; HCA/candidate executed bytes match declared sources.
- Results audit/ten-trials-h3/column-midpoint-10. No root source/EXE replacement, result_csv writes,30-trial batch, commit or push. No active process or further experiment scheduled. Do not claim BA superiority or silently install this candidate.

## Latest development: three recoverable BA candidates tested; none promoted

- User authorized developing BA for faster and more stable convergence. Tested three distinct audit-only variants: paired bisection with full-domain HCA recovery; recovery with matched pair nuisance variables/current-pair comparison; and adaptive full-domain allocation after ranges collapse. Detailed methods, all results, limitations and protocols: audit/ba-recoverable/DEVELOPMENT_REPORT.md. Root modBA remains BA=HCA; none met both speed and dispersion criteria, so do not install any silently.
- Each candidate had one fresh actual UI10x5000 comparison against unchanged HCA at H3/fc240. Total this development turn60 full runs across3 conditions (30 candidate/30 HCA), plus short mechanism tests. Do not pool these adaptive development conditions into a confirmatory30-trial research result; keep all outcomes. No candidate retries or fourth candidate/batch in this turn.
- Every method in all3 conditions reached observed target2942.34192 in10/10. Recovery candidate mean first-target BA371.5/HCA414.4, SD306.55/249.24: mean lower10.35% but dispersion worse. Matched candidate610.7/368.6, SD726.55/209.19: worse. Adaptive candidate704.3/384.1, SD537.20/225.40: worse. The requested joint faster/more-consistent objective remains unmet; no statistical superiority or guarantee claimed. Do not select only the favorable mean.
- Native mechanism checks11829/12030/13202 allPASS, including real recovery, feasible-only state sharing, raw-move/grouping equivalence for full-domain HCA, joint bounds, exact budgets, paired comparison, and impossible bearing. Each100000-record actual batch passes91 nativeCSV/UI/cost/first-index/safety/source/protected-output checks. All root VBP source hashes unchanged, global HCA neighbor clones byte-identical apart from private name, CRLF/no BOM. Evidence final-verification.json.
- No root source or EXE replacement, result_csv modification, commit or push. Handoff and audit additions only. Candidate folders ba-recoverable, ba-matched-recoverable, ba-adaptive-recoverable and corresponding ten-trials-h3 folders preserve all experiments. No active background work. Next methodological change awaits further work; do not claim a successful BA replacement exists.

## Latest test: immediate paired three-variable BA (audit only; not promoted)

- User authorized testing the proposed simultaneous tb/TBase/Base flow. Built audit/ba-paired-triple/candidate-modBA.bas: all-left versus all-right, one HCA-style proposal each, immediate comparison, strict feasible improvement before contracting all3 ranges. Separate regional incumbents; ties/no improvement keep parent; singleton geometry continues remaining-variable search. Three split variables always participate; other8 retain HCA grouping. No phase quota or mixed8-box search. Method details and limitations are in audit/ba-paired-triple/README.md.
- Native mechanism checks10872 PASS including actual joint contractions, fixed-range continuation, budget edges and impossible bearing. One actual GUI10x5000 batch per method at H3/fc240: audit/ten-trials-h3/paired-triple-10. BA reaches common observed target2942.34192 in5/10; HCA10/10. Conditional first-target means BA271.8 (only5 successes) and HCA454.5; these conditional means do not establish BA superiority. All-trial final mean BA3412.2236184 vs HCA2942.34192. All20 final designs pass configured checks. Full100000-candidate/nativeCSV/UI/source/protected-output verification91PASS.
- Reconstructed decisions from actual cost/validity traces agree with every observed fixed-mode transition: BA failed trials1/2 fixed0.35/0.40/2.00 from evaluations6/8; trials5/10 fixed0.25/0.35/1.50 from12/24; trial6 fixed0.30/0.40/2.00 from48. All5 target-reaching trials fixed0.20/0.30/1.50. This is decision reconstruction, not directly logged bounds. Evidence decision-reconstruction.json. Early permanent range selection is a concrete issue for this tested policy.
- No tuning, retries, extra ten-trial batches,30 trials, root source/EXE changes, or result_csv writes. Candidate is NOT installed; root modBA.bas still delegates to HCA as documented below. Handoff/audit additions only. No claim about all BA variants or statistical significance. Await next user instruction; do not silently promote or alter the tested candidate.

## Current user-requested control: BA runs exactly HCA (bisection disabled)

- Explicit latest request: make BA identical to HCA first and run ten times. This supersedes all BA policies below for the current installed code. Do not restore bisection or tune sampling without a new instruction.
- modBA.BisectionOptimization now delegates every search argument to modHillClimbing.HillClimbingOptimization with ReportAsBA=True, and copies completed history to CostHistory_BA. All obsolete BA-only search code is removed from the active module; the full previous module is preserved at audit/ba-hca-baseline/before-modBA.bas. Existing BA material helpers and CSV routines remain.
- HCA's only change is an optional reporting flag (default False) and BeginSearch label routing. Byte comparison after removing these two reporting edits exactly reproduces the prior HCA module. Initialization, full domain, shared random draw order, member-group selection, rejection/acceptance and evaluation budget are identical for both buttons. No production seed/replay API. Form progress identifies BA as HCA baseline; result labels/exports remain separate.
- Native parity test audit/run_hca_baseline_probe.py: before differs in cases2/3/4, after all4 cases identical, including every full design, validity, cost, global best, first-best index, final design and complete history. Covers budgets1/2/500 and128 impossible-bearing evaluations. Identical recorded natural moves exist only in audit snapshots and are not used in the actual ten-trial batch.
- Actual UI experiment audit/ten-trials-h3/hca-baseline-10: H3m/fc240, each method10x5000, natural independent random streams. Both10/10 reach2942.34192; BA first-best counts213,243,989,291,98,308,274,300,544,230 (mean349); HCA751,613,585,332,460,466,203,221,760,350 (mean474.1). ScreenBA98/HCA203. Counts differ despite identical search because actual random draws differ; this control does not demonstrate bisection performance or statistical superiority.
- All20 final designs pass configured validation including OT/SL/BC. Full100000-candidate/nativeCSV/UI/source/protected-output verification91PASS; root-source production compilePASS at audit/ba-hca-baseline/production. No tuning or repeat batch after seeing results. No30 trials, result_csv changes, root EXE replacement, commit or push. Run the root RC_RT_HCA_v2.vbp to use current source. Historical policy-specific tests below are superseded.

## Latest BA repair: preserve local progress and avoid premature region pruning

- User rejected the2/10 result and requested repair. Diagnosed full uniform bar redraw (8000 combinations, only1 minimum at fixed known H3 geometry), short3-proposal child search, infeasible local origins, and feasible DB/SP local traps. Global-best retention was checked and remains correct. Evidence: audit/ba-recovery/README.md; use neighborhood-diagnosis, not the initial invalid enumeration fixture.
- Promoted exactly candidate-v3-modBA.bas after mechanism checks and one actual10-trial candidate batch. DrawLocalMove_BA uses incumbent validity explicitly: broad existing-domain bar sampling until feasible; then diameter +/-1 and spacing +/-2 table-index moves from each incumbent. Geometry moves/clamps unchanged, every variable sampled together, no BA member-group freezing. These rules apply generally; no height/target-price conditional or production enumeration.
- Child searches no longer stop after a fixed small total. A phase is eligible to contract only when EVERY child has20*Countloop consecutive non-improving local moves; improvements reset that child's counter. Equal round-robin full-vector evaluation continues. Unique feasible cheapest child commits all3 bounds together; ties/no feasible/partial phase keep parent. Stagnation is a heuristic, not global-optimum proof. Full overall budget remains5000 including initial/reset/neighbor, with continued search after ranges collapse.
- Only modBA.bas changed in production this task. HCA/shared/formulas/checks/domains/units/prices/UI/CSV unchanged; CRLF/no BOM. BA now draws11 active values, HCA retains its existing grouped policy; do not claim identical neighborhoods or isolate bisection's effect from these comparisons. Never slow HCA to force a win.
- Tests: audit/ba-recovery/conditional-v3 confirms64 complete sampled candidates/local-vs-broad modes, PASS. final-control-flow from root:50460 checks PASS including2 actual joint contractions and3844 fixed-range continuation moves, budget edges, H4/H5 short checks and impossible bearing. Root-source compile PASS. Exact candidate hash matches tested snapshot. Audit-only v1/v2/phase-only/local-only candidates were not installed; reasons for rejecting them are documented.
- New actual UI experiment audit/ten-trials-h3/recovery-v3-10: BA/HCA each10x5000 natural randomness. BOTH reach2942.34192 in10/10. BA mean first-best2338 (min1541,max4294), HCA510.8 (min120,max972); screen BA1541/HCA120. All20 final designs pass configured checks.100000-record/nativeCSV/source/protected-output verifier91 PASS. Target attainment recovered in this sample; no claim of future100% success or BA speed superiority. Speed remains unresolved and older samples are not paired.
- No further tuning/retries after the10-trial result, no30-trial batch, no result_csv changes or root EXE replacement. Open root RC_RT_HCA_v2.vbp for installed source. Local/uncommitted changes retained.

## Latest BA correction: all sampled components used together (no member-group freezing)

- User again stressed simultaneous work with random sampling of the other variables. Found GenerateNeighbor_BA computed every sample but then SearchMoveIncludes restored inactive groups to the incumbent. Prior claims of full-vector sampling did not mean every sampled component was actually applied. Removed that overwrite block from BA only; all5 geometry samples and6 bar samples now participate together, with existing increments/clamps. Zero/repeated/clamped draws can still leave values unchanged.
- Only the BA gating block and two comments changed. Three-variable simultaneous partition/region/phase policy remains as above; no formula/unit/check/domain/price/UI/output changes. HCA and modShared untouched; same12 raw draws still occur, BA ignores the member selector. IMPORTANT: BA/HCA application policies now differ, so do not claim shared-neighborhood parity or attribute comparative results solely to bisection. Do not slow/change HCA just to force BA to win.
- Evidence audit/ba-full-candidate: native known-input regression5 failures before ->0 after, each full candidate through real evaluation once; production root-source compile PASS, exact byte-scoped change.diff/baseline, CRLF/no BOM. These forced moves exist only in diagnostic snapshots, not production or the10-trial experiment. Existing full-region semantics rerun at audit/ba-three-variables/full-candidate:12324 checks/18 phases PASS including acceptance/rejection, joint bounds, budgets and impossible bearing.
- Actual UI10x5000 per method: audit/ten-trials-h3/full-candidate-10. Natural randomness. BA reaches2942.34192 in2/10 (2105,2722); HCA10/10. Mean final cost BA3252.8969752 vs HCA2942.34192. Screen BA2105/HCA122. Other8 BA trials end2945.421984..3868.641424. All20 final designs pass configured checks. Full100000-record/nativeCSV/source/protected-output verifier91 PASS. BA superiority remains unproven; comparison includes different application of raw sampled moves and the existing per-region search allocation issue.
- No post-result tuning/retries, no30-trial batch, no result_csv changes, no root EXE replacement. Open root RC_RT_HCA_v2.vbp for current source. Local/uncommitted changes retained.

## Latest BA correction: three variables together with complete-vector random search

- User clarified all3 variables work simultaneously with random search of the remaining variables. Removed ActiveAxis_BA/nextAxis single-variable cycle. Implementation announced to user covers mixed lower/upper combinations to preserve the whole parent search region (8 children for3 splittable axes;4/2/1 as axes become fixed). This is the implemented interpretation; user did not explicitly request only all-low/all-high pairs.
- All3 child bounds are installed before one unchanged HCA-style full-vector proposal; each child owns a complete incumbent. Changed origins project all3 coordinates before one counted evaluation. Existing feasible strict-improvement acceptance and all configured checks remain. After equal full searches, select the unique cheapest feasible child and commit all3 ranges together. Ties/no feasible child/partial phases retain parent bounds. Global best remains independent of contraction; full-budget execution continues after ranges collapse.
- Total phase target remains20*Countloop, completed to a whole round-robin sweep: ceil(target/childCount)*childCount. Explicit initial H3 allocation is24 local moves /8 children =3 moves per child. Overall budget remains5000 actual evaluations including initial/reset requests. Limited early per-region sampling is a concrete issue for further investigation; do not silently change allocation or claim the performance issue solved.
- Only modBA.bas Section5 changed; HCA/shared sampler/formulas/checks/units/domains/prices/UI/output unchanged, CRLF/no BOM preserved. audit/ba-three-variables has exact baseline/change.diff, native red2->green0 test, semantics12629 checks/18 phases PASS, budget edges and impossible bearing, production root-source compile PASS. Prior single-axis scoped tests are superseded for current mechanism.
- Repeated actual UI experiment: audit/ten-trials-h3/three-variables-10, BA/HCA each10x5000 natural randomness. BA reaches2942.34192 in2/10; HCA10/10. Mean final price BA3243.2506296 vs HCA2942.34192. BA8 failures finish3093.18408..3579.591264. Screen BA292/HCA159. All20 final designs pass configured checks. Full100000-evaluation/nativeCSV/source/protected-output verifier91 PASS. Conditional successful-only mean excludes8 BA failures and cannot establish superiority. H3 performance remains unresolved.
- No post-result tuning or retries; no30-trial batch, no result_csv changes and no root EXE replacement. Open root RC_RT_HCA_v2.vbp to use latest code. Local/uncommitted changes retained.

## Latest user correction: all three BA variables must split simultaneously (pending clarification)

- User rejects the previous tb->TBase->Base cycle: all3 variables must be divided in the same phase. The last implemented single-axis policy is therefore not accepted as the intended method.
- Native mismatch repro: audit/ba-three-variables/before, python audit/run_three_variable_probe.py before. First local sample halves tb20..23 to20..21, but TBase40..42 and Base60..61 remain whole. FAILURES2 confirms the mismatch. One24-evaluation diagnostic only; production source hashes unchanged.
- Asked whether mixed low/high combinations across variables are allowed (8 subregions), or only all-low/all-high groups (2). Await this methodological clarification before selecting a replacement policy. Do not silently choose one or claim the three-variable correction is already implemented. No new10/30-trial batch, no result_csv writes or root EXE replacement in this diagnosis.

## Latest authorized BA fix: split first, independent HCA search in each half

- User authorized "fix and test as before" after the confirmed split-order mismatch. Kept existing tb->TBase->Base active-axis cycle (announced to user); no simultaneous8-box search. Only modBA.bas Section5 changed. Lower/upper halves now have separate local incumbents, costs and validity. Set half bounds before each unchanged HCA-style neighbor draw; alternate lower/upper steps; accept feasible strictly cheaper candidates and retain rejected incumbents separately.
- Half origins reuse a known checked design in that half or project the active coordinate into it and evaluate the changed design. Unevaluated origins are invalid. Known global best may be reused only within all active bounds. Keep initial conservative/midpoint evaluation behavior. Overall budget counts every actual initial/reset/neighbor evaluation.
- Existing total phase allocation20*Countloop is divided equally between both halves. Commit the cheaper feasible half only after a complete phase; ties/two invalid sides/partial phases keep parent bounds. Continue after all ranges collapse until full budget. Shared sampler, HCA, engineering formulas/checks/units/prices/domains, UI and output routines unchanged. This supersedes the previous single-incumbent RecordSide_BA phase policy and its scoped tests.
- Evidence: audit/ba-split-first/README.md, baseline-modBA.bas and change.diff. Native original mismatch21 failures ->0 after fix. Semantics probe12718 checks/23 phases PASS; budgets1/2/3/23/24/25, H3/H4/H5 and impossible bearing covered. Root-source production compile succeeds. Preserve original CRLF/no BOM; git diff check uses cr-at-eol.
- Authorized10-trial experiment: audit/ten-trials-h3/split-hca-10, actual BA/HCA buttons, each10x5000, natural randomness. BA reaches2942.34192 in7/10; HCA10/10. BA trials4/9 end3254.924928; trial7 ends3281.05368. Conditional target-reaching mean BA395.71429 (7 successes) vs HCA455.2 (10 successes); do not omit the3 BA failures in conclusions. Screen BA191/HCA106. All20 final designs pass configured safety. Full100000-evaluation trace/CSV/source checks91 PASS. This does not establish better overall BA performance; H3 reliability remains unresolved.
- No further trials or tuning after these results. result_csv hashes unchanged; no30-trial batch and no root EXE replacement. Open root RC_RT_HCA_v2.vbp for current code. All changes local/uncommitted.

## Latest authorized experiment: ten trials per method at H3/fc240

- User explicitly requested10 repeats. Ran actual Form1 BA and HCA button handlers with10 trials each and5000 actual evaluations per trial, natural automatic randomness, in isolated native snapshot audit/ten-trials-h3/current-10. No30-trial batch. No production code changes.
- Full20-trial result: BA reaches2942.34192 in9/10 trials; trial9 ends3281.05368 at868. HCA reaches2942.34192 in10/10. Screen reports BA150 (trial5), HCA149 (trial3). Screen is earliest minimum-price trial, not across-trial mean.
- README.md, trials.csv, complete50000-row traces per method, native screen text, summary.json, snapshot.json and verification.json preserve evidence. All20 final designs pass configured validation and OT/SL/BC thresholds; full100000-evaluation traces agree with summaries. Source hashes and user result_csv remain unchanged. No retries or tuning to favor BA. H3 speed/reliability issue remains unresolved; trial9 failure cause not yet traced.

## Latest BA performance correction: retain a better feasible midpoint incumbent

- User asked to find a way for BA to be faster than HCA. Diagnosed actual local-incumbent loss: midpoint handling replaced current even when the midpoint was worse or infeasible. Before native assertions fail2 development cases and9 fresh-stream events. Global best was safe, but local search could lose progress.
- Added ApplyMidpoint_BA in modBA.bas and used it at both reset sites. Evaluate every midpoint normally; accept it only if feasible and strictly better (or no feasible current exists). If both are invalid, a midpoint can remain an invalid search origin but never a passing result. Restore current indices when rejecting a midpoint. Record the actual midpoint, rather than retained current, in side observations.
- Only modBA.bas changed. HCA, the common local sampler, all validation/formulas/units/prices/outputs and no-seed behavior remain unchanged. Tested constant20 phase length but it did not improve development H3; reverted it. Active phase length is still20 * Countloop. No cost/height/seed-specific tuning or hidden evaluation counts.
- Evidence: `audit/ba-incumbent/README.md`, clean baseline source, before/candidate, fresh-stream holdout-before/after, edges, comparison.json and production/compile.log. `python audit/verify_ba_incumbent.py` passes78 checks; `audit/fresh-random/ba-incumbent/runtime.txt` passes31 actual UI/direct/randomness/export checks. Compilation succeeds from root sources. Historical policy-specific verifiers are superseded for current scope.
- Results: development H3 target2971.95792 drops BA322->118, HCA116. Fresh stream500 evaluations: H3 target2942.34192 BA224->224, HCA222; H4 target4985.56464 BA383->227, HCA373; H5 target10007.300956 BA64->64, HCA488, but BA final price worsens8920.74618->9398.28123. Do not hide that tradeoff or claim overall/statistical superiority. H3 faster-than-HCA remains unresolved.
- H3 has tb20–23, TBase40–42, Base60–61 while shared increments are±2/±5/±1. HCA already traverses these short domains rapidly. Do not shrink HCA's steps merely to force BA to win; changing the agreed common movement policy would require a clear methodological instruction.
- No30-trial research batch, result_csv writes or root executable replacement. All diagnostics are under audit. Reopen root `RC_RT_HCA_v2.vbp`. Changes remain local/uncommitted. Original user HCA70/BA100 were equal-price screen results atH3/fc240; associated CSVs were deleted.

## Latest correction: random search inside BA ranges must be HCA-style

- User corrected: "บีบแล้วสุ่มเหมือน hca นะ". The previous uniform low/high sampler did not meet this requirement. Removed GeneratePair_BA and its extra half-range Rand draws. Only modBA.bas changed in this task; original GenerateNeighbor_BA/DrawSearchMove behavior is preserved and its returned candidate is now evaluated unchanged.
- BA uses the same local acceptance rule as HCA: feasible and strictly cheaper, otherwise retain the previous anchor. A search phase uses20 * Countloop local neighbor evaluations (historical formula restored as the default and communicated to the user). Active midpoint comparison cycles through tb/TBase/Base, skipping singleton ranges. Record best feasible evaluated designs on both sides, including a known incumbent/global best only if inside all current bounds.
- Choose a half only after a complete local-search phase with observations on both sides. Ties/two infeasible sides/missing observations/partial final phases do not contract. There are no forced pairs or two independent walkers now. First midpoint initialization remains; later active-coordinate midpoint changes are real counted reset evaluations. Other dimensions continue local search after the three ranges collapse. All actual evaluations, including rejected ones, count to full-budget termination.
- This supersedes the prior paired-uniform sampling implementation and its per-pair contraction timing. Engineering formulas/units/prices/HCA/shared helper/UI/output routines remain unchanged. Seed controls remain removed. Old private MidPrice fields are inactive.
- Evidence: `audit/ba-local-search/README.md`, native `before` failure versus `verified` pass, `edges`, `odd3`, `production/compile.log`. Native instrumentation confirms every actual BA random proposal equals the unchanged neighbor generator output. Phase/budget/range tests pass. `audit/fresh-random/ba-local-search/runtime.txt` passes31 real GUI/direct/randomness/export checks. `python audit/verify_ba_local.py` passes158 trace/source checks. Root-source compile succeeded.
- Report negative evidence honestly: in the recorded H3/fc240500-evaluation check, BA reaches2971.95792 at322 and HCA the same price at116. Earlier paired BA reached2942.34192 at67. The revised mechanism has not demonstrated a speed gain and did not reach that earlier lower price in this short check. H4/H5 outcomes are recorded in README. Do not claim performance fixed or tune HCA/target-specific rules to force BA superiority.
- No30-trial research batch, result_csv change or root EXE replacement. All diagnostic outputs are under audit. Reopen root `RC_RT_HCA_v2.vbp` to use current source. The user's original HCA70/BA100 were equal-price screen results atH3/fc240; CSVs were deleted. Current scoped verifier is `verify_ba_local.py`; older paired verifiers are historical. Changes remain local/uncommitted.

## Latest user-specified BA algorithm: paired midpoint sides

- User explicitly defined BA as searching both sides of a midpoint, comparing feasible answers, retaining the better half and repeating through the full budget; then asked to implement it. This supersedes the old historical-MidPrice contraction and20 * Countloop schedule. The earlier small midpoint/recovery corrections alone were insufficient.
- Only modBA.bas changed in this task. It now visits tb/TBase/Base cyclically, comparing a lower/upper pair for one active coordinate before any anchor or bound change. Other values share one existing random background proposal within each pair, apart from required tt<=tb repair/derived heel. BA samples its active coordinate in both discrete halves using Rand. HCA and the common random helper remain unchanged.
- Discrete partition: split=(lo+hi)\2, lower[lo,split], upper[split+1,hi]. It covers all available indices; the integer midpoint belongs to the lower half. This coordinate-wise interpretation avoids comparing only all-low versus all-high corners. It is documented in `audit/ba-paired/README.md`; do not claim exact reproduction of the one-variable staircase study.
- Best known feasible incumbent participates in its own side. Retain the strictly better side only after both probes; ties and two rejected probes keep bounds. Alternate which side is evaluated first. A final incomplete pair can improve global best but cannot contract. All actual initial/midpoint/lower/upper/neighbor evaluations count. When all three ranges collapse, continue the existing search of remaining variables through the full budget.
- Original initialization, shared-input API, background GenerateNeighbor_BA and CSV functions are preserved. Form1/HCA/modShared/formulas/prices/units are unchanged. Automatic no-seed randomness and no trial directories remain. Old private MidPrice variables are inactive; they no longer control BA.
- Native evidence: `audit/ba-paired/before` fails explicit-side checks; `verified`, `replay`, `edges`, `odd3`, `odd5` pass. Both side directions, tie/rejection cases, interval membership, shared backgrounds, continued search and budgets are checked. `audit/fresh-random/ba-paired/runtime.txt` passes31 actual UI/direct/randomness/export checks. `audit/ba-paired/production/compile.log` compiles root sources. `python audit/verify_ba_paired.py` passes664 repeated native-trace/source checks, not664 trials.
- Captured H3 path: previous BA2942.34192 at59, new paired BA same price at67; HCA2971.95792 at116. H4/H5 prices differ and are fully recorded in README. No demonstrated overall speed gain or statistical claim; user will run/report the experiment. Do not tune results or slow HCA. User's original HCA70/BA100 at equal price came from screen atH3/fc240; CSVs were deleted.
- All diagnostic writes are isolated under audit. No30-trial research batch or result_csv change; root executable not replaced. Reopen root `RC_RT_HCA_v2.vbp` to run current source. `audit/ba-paired/final` is a failed intermediate audit-helper build; use `verified`/`replay`/`production` evidence. All changes remain local/uncommitted. Older policy-specific checks are historical and are superseded by `verify_ba_paired.py`.

## Latest BA revision: retain feasible collapsed intervals

- User confirmed H3/fc240, equal final prices and HCA70/BA100 from the screen; they deleted the CSVs. They explicitly requested another code revision and will run/report the experiment themselves. Do not ask again whether these reported prices are equal or claim the deleted paths were reproduced.
- Native inspection found BA reopened all three root ranges whenever they collapsed, even with a feasible current round. H3 diagnostic ranges20/40/60 reopened to20–23/40–42/60–61 three times. Removed the all-three-collapsed trigger from the recovery condition in modBA.bas, adding one comment. Feasible collapsed geometry stays fixed; tt/LToe/six steel indices keep searching. Invalid rounds still reopen root bounds.
- This supersedes the previous preservation of feasible-collapse recovery. It changes that BA recovery behavior only; first-round midpoint use, midpoint arithmetic, price-based bound updates and20 * Countloop remain. HCA, shared moves, no-seed automatic random stream, actual evaluation counting, engineering formulas/units and output behavior are unchanged. Retention of a feasible local geometry is not proof of global optimality.
- Evidence: `audit/ba-contraction/README.md`, before/final/edges native logs/traces, `verification.json` (69 checks) and `production/compile.log` (successful root-source compilation). The native range test fails before and passes after. HCA traces are identical; H4/H5 invalid-round recovery is unchanged. Budget1/budget2/no-solution checks pass. No diagnostic hooks exist in production.
- H3 recorded diagnostic BA best remains2942.34192 at evaluation59 before/after; thus no convergence speed gain has been demonstrated by this small test. The removed reopening happens after the best was found in that path. Do not present this as resolution of the user's70/100 observation or a statistical performance claim. Wait for the user's actual experiment results; never force BA to win by tuning HCA or target-specific rules.
- No30-trial batch, result_csv edits or root executable replacement. All diagnostic writes stay under audit. Reopen root `RC_RT_HCA_v2.vbp` to use current source. Changes remain local/uncommitted. Current scoped verification is `python audit/verify_ba_contraction.py`; older scripts/source assertions refer to their historical snapshots and may fail against this deliberate later change.

## Latest BA correction: apply the midpoint in the first search round

- User again requested BA diagnosis/correction and reported HCA70 versus BA100. H3/fc240 are inferred from prior context; price equality and whether the counts came from UI/CSV remain unconfirmed. No latest primary CSV was present. Do not claim this exact observation was reproduced or resolved.
- Native checks showed BA computed midpoints but searched its first20 neighbors from the upper-bound initial design because of `Countloop > 1`. Removed that exclusion in modBA.bas so a changed midpoint is evaluated before first-round neighbors, consistent with the supplied staircase article's midpoint-before-neighborhood ordering (printed page77). The conservative initial evaluation and global-best retention remain; all actual evaluations count normally. Unchanged midpoint reuse remains.
- This task changes only that guard and its comment in active production sources. HCA, automatic random initialization/no seed controls, shared neighborhood, midpoint arithmetic, later contraction/price/recovery rules, engineering formulas and units are unchanged. This is an ordering correction, not a guarantee of improved convergence or complete equivalence to the staircase algorithm.
- Evidence: `audit/ba-first-midpoint/README.md`, before/final native traces, `verification.json`, edge-case traces and `production/compile.log`. First-center checks fail before and pass after at H3/H4/H5; HCA traces are byte-identical. Budget1/budget2/impossible-bearing checks pass. Diagnostics replay recorded move proposals only inside isolated snapshots; no production replay or seed API was added.
- Report performance honestly: in the recorded H3 path BA reaches the same2942.34192 at evaluation59 after versus58 before. H4/H5 BA final prices are worse within their64-evaluation checks. Thus this change has not demonstrated a speed improvement or explained the user's70/100 pair. Do not tune search rules to force BA to win. Further diagnosis requires the corresponding experiment context/results.
- No fresh30-trial batch, result_csv writes or root executable replacement. Reopen `RC_RT_HCA_v2.vbp` to load current source. All changes remain local/uncommitted. Older evidence and assertions refer to their historical source versions; use `python audit/verify_ba_midpoint.py` for the current scoped correction.

## Latest UI change: center the main window on opening

- User requested centering the program window whenever it opens. Form1.frm now sets native VB6 `StartUpPosition = 2` (CenterScreen). All other form bytes were preserved for this change; no calculation, random-search or output changes were made.
- Root-source compilation succeeded via `audit/center-window/MainCompile.vbp`; see `audit/center-window/compile.log`. No optimizer or30-trial batch was run. Reopen root `RC_RT_HCA_v2.vbp` to load the new form property. Root executable was not replaced.

## Latest user-authorized change: remove seed controls and fixed replay

- The user explicitly requested removing Seed after observing identical results from repeated clicks. This overrides all older requirements below for fixed seeds, seed-per-trial scheduling and reproducibility from a user-entered seed.
- Removed txtSeed/lblSeed, seed validation/status text/comparison-key fields, RandomSeed optimizer parameters, RunSeed state and fixed per-trial resets from every active VBP source. Batch calls and optional diagnostic report columns were updated too. Old backup files and historical audit snapshots were not changed.
- `InitializeSearchRandom` calls parameterless VB6 Randomize once per process and is guarded by a Static Boolean. Form_Load initializes before random concrete selection; BeginSearch also initializes for direct optimizer callers; the batch random-material path uses the same initializer. Trials and subsequent clicks continue the PRNG stream, with no fixed seed or per-trial restart.
- Both methods still use exactly the existing DrawSearchMove/SearchMoveIncludes/Rand rules, initial designs, BA midpoint/recovery rules, acceptance/global-best logic, evaluation accounting and engineering formulas/units. No claim that every resulting price or best-found iteration must differ: identical optima remain possible under different random paths.
- Evidence: `audit/fresh-random/final/runtime.txt` passes31 native checks; `audit/fresh-random/verification.json` passes72 verification checks; `audit/fresh-random/production/compile.log` compiles root sources successfully. The regression drives actual Form1 BA/HCA buttons twice each, two trials per click and64 evaluations per trial, plus two direct64-evaluation calls per method. It records candidate sequences, not just final prices. Before removal repeated sequences fail; after removal sequences differ across clicks, trials and direct calls.
- All diagnostic outputs are isolated under audit; result_csv was not written, no new30-trial batch was run, and the root executable was not replaced. Reopen root `RC_RT_HCA_v2.vbp` to load the current source in VB6. Changes remain local and uncommitted.
- Earlier seed-based probe scripts/API calls and their source-scope assertions describe historical versions and may no longer compile/pass against current sources. Preserve their evidence; use `python audit/run_fresh_random_probe.py <new-label>` and `python audit/verify_fresh_random.py` for current behavior. Do not reintroduce fixed replay merely to satisfy older tests.

## Latest local update: H3 convergence diagnosis

- User reported HCA faster than BA at H=3, fc'=240 after 30 trials and authorized analysis/correction of code defects. Do not force BA to win or change engineering/search policy to fit the hypothesis.
- Existing H3 loopPrice files (read only) have 30 equal-price results at 2942.34: BA mean Loop=365, median=270; HCA mean=413.7667, median=297.5. BA is earlier in 11 pairs, HCA in 19. Last trial seed12374 is BA Loop238 vs HCA234. This is not evidence that BA is slower on average or that either method must win every seed.
- Native reproduction confirmed the last-trial gap before AND after the output-folder change, with identical candidate traces. Four BA resets reevaluated an unchanged incumbent at evaluations22,63,124,205. This is unnecessary computation, not new exploration.
- Added a guard in `modBA.BisectionOptimization`: reuse current cost/validity when midpoint tb/TBase/Base and tt repair leave the design unchanged. Real midpoint changes and tt<=tb repair still evaluate normally. No evaluations are hidden; avoided calls are replaced by actual neighbor evaluations within the same budget. Random stream, neighbor rules, contraction decisions, HCA, engineering formulas and units are unchanged by this task.
- Final bounded H3 diagnostic results, 1-based evaluation counts: seed12374 BA239->235, HCA235; seed12345 BA283->280, HCA299. All reach2942.34192. These are development checks, not a fresh 30-trial comparison or proof of universal BA superiority.
- Evidence: `audit/h3-convergence/README.md`, `verification.json` (36/36 checks), `final-production/compile.log` (root-source compile succeeded). `audit/diagnose_h3_convergence.py` creates fresh isolated native snapshots; `audit/check_ba_reset_trace.py` fails on old redundant resets and passes after correction; `audit/verify_h3_convergence.py` verifies traces and source scope.
- Other bounded checks cover genuine H5 midpoint transitions, impossible bearing/no-solution, and budget1. All diagnostic writes remain under audit. Root executable was not replaced. User result_csv remains untouched; no new 30-trial batch was run. Changes remain uncommitted.

## Current local update: stop per-trial folders (user requested)

- The user subsequently authorized stopping automatic `BA-<timestamp>-seed...` and `HCA-<timestamp>-seed...` folders. This supersedes the older per-trial folder/report behavior described below.
- Local source changes: BeginSearch no longer creates a run folder; evaluation-detail buffering and automatic `evaluations.csv`/`run.txt` exports are removed. Implicit trial-summary export is skipped when no run folder or explicit summary path exists. UI no longer advertises these reports.
- Each trial now writes the fixed primary accept CSV through the existing archive-preserving writer. Previous trials remain in `archive`; primary loopPrice CSV behavior is retained. No user-owned result files or old folders were modified/deleted.
- Formulas, search algorithms, units and engineering scope remain unchanged. Preserve VB6 source encoding, CRLF and no BOM.
- Evidence: `audit/no-trial-folders/compile-main.log` compiles current root sources successfully. `runtime.txt` reports FAILURES=0 for an isolated native export probe; `verification.json` confirms only primary CSVs plus archive and preserved trial copies.
- The probe uses a modShared copy differing only in its output-root constant, redirected beneath audit. It exercises two export cycles per method, invalid candidate evaluation and synthetic CSV rows; it is not an optimizer benchmark or a 30-trial experiment. No root executable was replaced; compiled executables are under audit.
- These code changes are local and uncommitted. Two pre-existing untracked research_figures files are unrelated and must not be included without user direction.

## Workspace and checkpoint

- Work directly in `C:\reserch 69\RC_Wall_Fix` only; remote `https://github.com/dohcivil-ui/RC_Wall_Fix`, branch `main`.
- Latest code checkpoint: `d0f399a`, pushed and pulled successfully. This is an empty documentation checkpoint following `ee09e26` (kg/cm^2 labels), `030f4fa` (original ton labels), and `254cbba` (multiline TextBox).
- Checkpoint message: แก้ไขช่องผลลัพธ์เป็น TextBox หลายบรรทัด ตัดบรรทัดอัตโนมัติ เลื่อนอ่านและคัดลอกได้ แต่แก้ข้อความไม่ได้ ใช้หน่วยตามโค้ดคำนวณมาแสดงผล
- No pending code change at handoff. The user has NOT specified a new implementation task; read this, inspect status, then await the next instruction.
- Read AGENTS.md and JIT_code_audit.md if present (not found in earlier checks), then the root `RC_RT_HCA_v2.vbp` to identify active files.
- User-owned `result_csv` files have many deletions and untracked experiment archives. Do not stage, restore, delete, overwrite, or include them in commits. Stage only task-owned files explicitly.
- Preserve original VB6 byte encoding, CRLF and no BOM. Use byte-preserving patches, not UTF-8 rewriting of .frm/.bas. No large refactoring or unsolicited scope expansion.

## Completed UI and unit work

- `Form1.frm`: `txtResults` is a native VB.TextBox replacing lstResults; MultiLine=True, ScrollBars=2 (vertical), Locked=True, Enabled=True. Original size, position and CordiaUPC font retained.
- `AddResultLine` appends original text plus vbCrLf using SelStart/SelLength/SelText. No manual 48-character wrapping. Clear, comparison and both optimizer result paths use txtResults. BA/HCA completion sets SelStart=0.
- `modShared.FormatScreenResults` presents old-style sections: materials, optimization, price, dimensions, pressures, weights, reinforcement, safety factors, bearing, member stresses and notes. Detailed run.txt remains available.
- User explicitly requires simple original display units. Current labels: `ton`, `ton-m`, `ton/m2`, **`kg/cm^2`**. Do not restore `tf`, `tf/m`, `tf.m/m` or `ksc` display labels. The calculation is per 1 m of wall length, and kg in stress labels means kilogram-force. These were STRING-ONLY edits, with no numerical conversion.
- Input concrete-strength label also says kg/cm^2. SectionStresses uses depth*100 (cm), 100 cm section width, and moment*100000 (kg-force.cm).
- As_min is actual minimum steel, not flexural As_req; do not mislabel it. Envelope quantities and full-weight nominal bearing case are distinguished.

## Engineering and pricing scope already agreed

- H and H1 measured from base underside. Full-wall active/passive use H and H1, resultants at H/3 and H1/3. Stem calculations use H-TBase and H1-TBase of each candidate. Full active AND passive are required for this project.
- Reference H=5, TBase=.30, H1=1.20, soil unit weight1.8, phi30: stem4.70m, active moment10.3823, net with full passive9.7262 ton-m. Stem .20m, clear cover .075m, DB12@.25 must be rejected.
- SD40, fy4000, fs1700; fc allowable .45fc'; n=2040000/(15100*sqrt(fc')). Actual selected DB and spacing determine steel area and effective depth.
- Current basis PROJECT_WSD_ACI99_V3_MAIN_ONLY (provided reference plus ACI99 supplement). This is NOT certification of all EIT2562 requirements. Consult existing source notes for criteria; never invent standards.
- Stability thresholds in code: overturning >=2, sliding >=1.5, bearing qa_allowable/qmax >=1. Use actual VB6 inputs, not potentially incorrect diagram labels. 16 vertical dead-load factor .85/1.0 combinations are checked with full active/passive.
- Price includes all structural concrete (stem+base) and main stem/toe/heel steel only. Excludes anchorage, laps, secondary/horizontal steel and formwork, per explicit user direction. Do not add these or tune formulas to retain a price.

## Search, CSV and final picture constraints

- BA contracts ONLY tb, TBase, Base, never five variables. tt, LToe and reinforcement remain in shared random search.
- BA/HCA use the same MEMBER_WEIGHTED_V1 random movement helper DrawSearchMove/SearchMoveIncludes. BA-specific contraction bounds and deterministic midpoint steps remain. Do not reintroduce HCA-only steel sweeps, hardcoded target designs/prices or artificial delays.
- Evaluation budgets include initial/reset/neighbor checks; global best updates from all feasible entry paths. Seeds repeat runs. No-solution is explicit.
- Both have found H5 cost8538.41892 in bounded native evidence, with different seeds. BA is not guaranteed faster every trial; neither heuristic guarantees global optimum. Do not rig results to make HCA slower.
- Native current-policy results: `audit/h5-search/release/README.md`, `bench.txt`, `holdout.txt`, `verification.json`. H5 HCA seed12374 found8538.41892 at evaluation3162; BA seed12345 found it at4196. These DIFFERENT seeds are not a fair direct convergence comparison. `audit/h5-search/final` is an UNSHIPPED older policy; do not treat it as release evidence.
- CSV root `C:\reserch 69\RC_Wall_Fix\result_csv`. Primary names accept-BA/HCA-H3/H4/H5.csv and loopPrice-BA/HCA-H3/H4/H5.csv.
- accept schema: No.,Rejected,Passed,Passed and Better value (last trial in primary; individual trial copies preserved). loopPrice schema: No.,Loop,BestPrice, one row per trial. CSV row/evaluation indices are zero-based; internal evaluations one-based. Keep buffering/performance fixes and archives.
- Final picture appears once after all trials. Choose feasible lowest cost, tie-break on fewer evaluations to first find it. Native frmBestDesign popup with OK/X, SVG10x8cm, outlined geometry, dimensions and steel circles with DB/spacing leaders, method and price; no PNG, earth fill, force arrows or footer.

## Verification evidence and tooling

- VB6 compiler installed: `C:\Program Files (x86)\Microsoft Visual Studio\VB98\VB6.EXE`.
- Latest compile: `audit/unit-labels/compile-kgcm2.log` succeeded against root sources via `audit/unit-labels/MainCompile.vbp`. This compile includes TextBox and final kg/cm^2 labels.
- Actual VB6 TextBox runtime test: `audit/results-textbox/runtime.txt`, FAILURES=0. H4/H5 report text preserved; native wrapped line count7 increased to10 on narrowing; locked/enabled and selected-text checks succeeded. Test loaded real Form1 invisibly, not a screenshot test or optimizer run.
- This runtime evidence predates unit string changes; after those, only compilation and source comparisons were rerun. Do not claim a new runtime or fresh 30-trial test.
- `python audit/verify_results_layout.py` verifies historical native evidence and current presentation-only scope, normalizing authorized unit label changes. It is NOT a new native runtime test.
- Before TextBox: historical `audit/results-layout` native ListBox evidence had38 clipped rows before manual wrapping,0 after. Keep historical reports intact; do not overwrite them to match new labels.
- No fresh 30-trial research batch is authorized; user prefers running those themselves. Bounded diagnostic tests only when justified. Keep outputs isolated under audit, never result_csv.
- Do not run old change_results_textbox.py or results_layout_edit.py blindly: they are one-time migration/prototype scripts, not current application entry points.
- If VB6 IDE holds stale project files open, reload them; do not save stale editor state over current disk source. Compile separate audit projects with absolute source paths; don't interrupt user IDE.
- For future work: state what actually compiled/ran, give evidence, and list limits. Do not claim PASS without evidence or imply that UI checks certify structural safety.

## Suggested first prompt in the new chat

ทำงานต่อในโปรเจกต์ C:\reserch 69\RC_Wall_Fix โดยใช้โฟลเดอร์นี้โดยตรง อ่าน HANDOFF.md และ AGENTS.md ถ้ามีก่อน แล้วตรวจ git status และไฟล์ .vbp ยืนยันสถานะล่าสุด ห้ามแตะผลทดลอง result_csv ห้ามเปลี่ยนสูตร อัลกอริทึม หน่วย หรือขอบเขตงานเพิ่มเติมเอง และยังไม่รัน 30 trials เมื่ออ่านครบแล้วสรุปสถานะสั้น ๆ และรอคำสั่งงานถัดไปจากผม
