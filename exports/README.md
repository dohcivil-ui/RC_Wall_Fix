# ภาพเวกเตอร์จากผล VB6

- `best_BA_trial1_10x8cm.svg`: ขนาดล่าสุด 10 × 8 ซม. เพิ่มขนาดข้อความและรูปเมื่อพิมพ์ โดยรักษาสัดส่วนกำแพงและวงกลม ไม่ยืดภาพ
- `best_BA_trial1_7x6cm.svg`: เวกเตอร์จริงพร้อมกรอบป๊อปอัป แถบชื่อ ปุ่ม OK และกากบาท (เส้น วงกลม และข้อความ ไม่มี bitmap ฝังใน SVG) ขนาดเอกสาร 7 × 6 ซม. SVG ไม่ขึ้นกับ DPI
- ส่งมอบเฉพาะ SVG ตามคำขอล่าสุด นำ PNG ของตัวอย่างนี้ออกแล้ว ปุ่มในไฟล์ SVG เป็นส่วนของภาพ; ปุ่มในหน้าต่าง VB6 จริงยังใช้งานได้ตามเดิม

ข้อมูลมาจาก `audit/results/BA-25690909-000943-seed20260908/run.txt`: ผลทดสอบ VB6 เดิม BA 1 trial, 64 evaluations ไม่ได้รัน optimization ใหม่ ไม่ใช่ผลทดลอง 30 trials วงกลมเป็นสัญลักษณ์ของชั้นเหล็กหลัก ไม่ใช้จำนวนวงกลมถอดปริมาณ ขนาดและระยะที่ป้ายมาจากรายงาน

ตัวส่งออกอยู่ใน `audit/export_best_svg.py` รายการตรวจองค์ประกอบ SVG และ SHA256 อยู่ใน `export-verification.json` ไม่มีการเปลี่ยนซอร์ส VB6 ในงานส่งออกไฟล์นี้
